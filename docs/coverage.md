# Clean-room coverage matrix

This document tracks the target behavior observed from four Office/PDF artifact workflows and the current implementation status of `open-office-artifact-tool`.

Status legend:

- `done`: implemented and covered by smoke tests.
- `partial`: API or minimal behavior exists, but fidelity/edge cases remain.
- `todo`: not implemented yet.

## Cross-cutting agent runtime

| Requirement | Status | Notes |
| --- | --- | --- |
| Shared `FileBlob` with `load`, `save`, `arrayBuffer`, `text` | done | Used by all four artifact families. |
| `inspect(...)` emits bounded NDJSON snapshots | partial | Implemented for workbook, presentation, document, and PDF—including normalized PDF table-cell and explicit/effective reading-order records—with shared `search`/`searchTerm` filtering, `target`/`targetId`/`id`/`anchor` slicing against stable IDs/common anchors, `before`/`after`/`context` windows around target matches, and top-level field shaping via `include`/`fields` plus `exclude`/`omit`. Needs full token coverage and richer semantic context slices. |
| Stable anchor IDs + `resolve(...)` | partial | Implemented for workbook/windows/source-bound connections/sheets/tables/charts/images/sparklines/rules/comments, presentation theme/layout/slides/shapes/text ranges/tables/charts/images/connectors/comments, document theme/blocks/text ranges/bookmarks/headers/footers/comments/styles, and PDF pages/text/text-items/reading-order entries/regions/tables/table-cells/images/charts. Needs richer imported-object identity preservation. |
| `help(query, opts)` bounded NDJSON API discovery | partial | Shared `HELP_CATALOG` and `helpArtifact(...)` power Workbook/Presentation/Document/PDF help methods. All 255 current catalog records have machine-readable parameter/return schemas: 151 Workbook/formula, 40 Presentation, 34 Document, 19 PDF, and 11 shared runtime records. Presentation help documents native-object name/outer-frame editing plus the narrower source-bound OLE XLSX payload read/replace boundary, including its intentionally retained preview image. Primary/additional workbook-window APIs, active/selected worksheet APIs, visibility, and sort state document bounded native worksheet/view contracts, while `workbook.connections` documents the source-bound database connection-root contract without exposing secret provider data. `test/help.mjs` enforces 100% schema coverage, and `docs/api.md` is generated with field-level parameter/return details. Catalog breadth still needs to grow alongside future APIs and formula coverage. |
| Render/preview loop | partial | `renderArtifact(...)` plus per-artifact render/export adapters return SVG `FileBlob` previews with metadata for workbooks, presentations, documents, and PDFs. `renderArtifact(..., { format, renderer })` supports pluggable PNG/WebP/JPEG/PDF conversion adapters over the SVG/FileBlob source; optional adapters include Playwright SVG/HTML → PNG/WebP/JPEG/PDF, sharp SVG/PNG/JPEG/WebP → PNG/WebP/JPEG, node-canvas SVG/PNG/JPEG/WebP → PNG/JPEG, Poppler PDF → PNG/PPM/TIFF page rasterization, LibreOffice DOCX/XLSX/PPTX/HTML → PDF/office-supported conversions, and native Office via the Node/C# JSON bridge. Playwright + Chromium PNG/WebP/JPEG/PDF smoke tests pass locally on macOS arm64, including Chromium canvas-backed WebP encoding. Raster adapters hard-error when their optional peer dependency or CLI is missing rather than silently faking output. |
| Shared verification API | partial | `verifyArtifact(...)` plus per-artifact `verify()` methods emit bounded semantic/layout issues. `visualQaArtifact(...)` adds PNG/JPEG/WebP/PPM decoded-pixel metrics, cross-encoding comparison, decoded-pixel limits, suppression of byte-only false positives, and valid PNG `diffBlob` heatmaps. Heatmaps accept changed/unchanged RGB+alpha palettes and can align dimension-mismatched images strictly, top-left, or centered on a union canvas. Optional same-size pixel registration searches a bounded baseline translation with a capped sample budget, applies only above a configurable improvement ratio, ignores only translated edge pixels, and records sampled/exact before-after metrics plus the chosen offset in summary/blob metadata. All four runnable skills share one shipped baseline-set module for model/native PNG loading, writing, stale-file cleanup, diff persistence, and page/slide-count evidence. Supplying `baselineDir` is fail-closed: missing, empty, or non-contiguously numbered sets are rejected instead of silently skipping comparison. Registration CLI flags and heatmaps remain artifact-specific workflow outputs. Needs broader cross-platform baseline evidence and, eventually, non-translation registration. |
| Layout JSON export | partial | Slide layout JSON, document page-aware layout JSON, workbook/worksheet layout JSON, and PDF page layout JSON are implemented. All four layout families now support target/search/context slices with page/element/sheet pruning. Workbook layout cells include raw calculation `value` plus deterministic number/date-formatted `displayValue`, styles, merges, and pixel bounding boxes alongside tables, charts, images, sparklines, and rules; PDF layout JSON includes page text, positioned text with H1-H6 semantics, reading order, regions, tables with origin-cell spans/roles/header IDs/bounding boxes, and image/chart alt/decorative semantics. Needs richer pagination/windowing and deeper semantic layout slices. |
| Durable file export/import smoke tests | done | Minimal XLSX/PPTX/DOCX/PDF round trips pass. |
| Safe reusable OOXML package layer | partial | XLSX/PPTX/DOCX inspect and patch APIs share bounded/path-safe package handling, content-type/relationship synchronization, namespace-aware source-reference validation, deletion cleanup, explicit-ID collision gates, and atomic final validation. Opt-in `sourceReference` builders add/remove DOCX settings semantics, header/footer references, batch classic-comment anchors, and paragraph numbering assignments; XLSX worksheet/table lists, worksheet drawings, DrawingML image/chart anchors, workbook pivot-cache lists, and cache-definition record IDs; plus PPTX slide/master/layout ID lists and image/chart objects in slide shape trees. Format decisions continue moving behind family modules. Dedicated deep modules own XLSX Office 2019 threaded comments, PPTX Office 2021 modern comments, DOCX cross-generation comment identity/people graphs, DOCX bookmark/internal-link planning, and DOCX section/header-footer declaration, activation, and inheritance. Arbitrary-path fixtures exercise add/remove/error/import/native-render paths. Needs richer pivot features and other structurally complex references. |

## Spreadsheets skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| XLSX line-series direct markers | done | Public series field 7 carries presence-aware `SpreadsheetChartMarkerArtifact`; marker fields 1–4 carry symbol, optional size, direct untinted RGB fill, and the reusable bounded RGB/dash/width outline. JavaScript exposes `marker: { symbol?, size?, fill?, line? }` on line series. The ten-symbol catalog is `none`, `dot`, `circle`, `square`, `diamond`, `triangle`, `x`, `star`, `plus`, and `dash`, with optional integer size 2–72; marker fill is `#RRGGBB`, and marker line reuses the five preset styles and 0–1,584 point width. `XlsxChartSeriesMarkerCodec` authors/imports/patches one direct `c:marker` plus exact bounded `c:spPr`, includes all marker style semantics in source hashes, and allows add/change/remove on recognized fixed-topology source-bound charts. Theme/transformed/gradient/picture marker graphs, extensions, duplicates, unknown children, and markers on non-line charts fail closed; complex imported marker graphs make the complete chart read-only and exact-preserved. Public wire presence, malformed-wire, JS fallback OOXML, SVG preview, verify, C# Office 2021, bundled-WASM, source-bound exact-roundtrip, add/remove, and runnable `open-chestnut-basic` tests cover styled diamond→triangle and star→plus profiles across two codec passes. |
| XLSX chart-level line grouping, smoothing, and varied colors | done | Public chart field 15 carries `SpreadsheetChartLineOptionsArtifact`; JavaScript exposes line-only `lineOptions: { grouping?, smooth?, varyColors? }`. Grouping is `standard`, `stacked`, or `percentStacked`, omission defaults to native standard, and SVG preview renders every series with deterministic stacking/normalization and optional smooth paths. Smooth retains explicit true/false presence. `varyColors: true` authors direct `c:varyColors val="1"`; false or omission normalizes to no optional node, while a recognized native false node imports as editable false. `XlsxChartLineOptionsCodec` authors/imports/patches the required plot-level `c:grouping` plus optional `c:varyColors` and `c:smooth`, includes effective grouping/vary-colors and explicit smooth presence in source hashes, and allows safe changes on recognized fixed-topology source-bound charts. Missing/unknown values, duplicate/attributed/nested nodes, invalid public input, extra options, and non-line use fail closed; an unrecognized native grouping/smooth/vary-colors profile makes the complete chart read-only and exact-preserved. Public enum/field/presence/malformed tests, JavaScript fallback import/export, model verify/SVG, C# Office 2021 validation, bundled-WASM source-bound editing, and `open-chestnut-basic` stacked→percentStacked, smooth true→false, and vary-colors true→false roundtrip cover the bounded slice. |
| XLSX plot-level value/category/position data labels | done | Public chart field 16 carries presence-aware `SpreadsheetChartDataLabelsArtifact`; optional data-label field 3 is an independently designed enum for `bestFit`, `bottom`, `center`, `insideBase`, `insideEnd`, `left`, `outsideEnd`, `right`, and `top`. JavaScript exposes boolean shorthand or `{ showValue?, showCategoryName?, position? }`, accepts the corresponding OOXML short aliases, preserves an explicit all-false block, and distinguishes explicit `bestFit` from omitted position. Both JS and OpenChestnut author canonical `c:dLbls` with an optional leading `c:dLblPos` followed by direct `c:showVal` and `c:showCatName`; SVG/inspect expose the same projection and deterministic bounded placement. `XlsxChartDataLabelsCodec` imports, authors, hashes, and source-patches label-block and position add/change/remove operations on recognized fixed-topology bar/line/pie charts, while retaining false standard residual flags. Unknown/duplicate/attributed/nested owned scalars, visible legend/series/percent/bubble flags, per-series/per-point overrides, formatting, text styles, leader lines, extensions, and unknown children make the complete native chart read-only and exact-preserved. Public wire presence/unknown-enum tests, malformed JS input, fallback OOXML aliases/rejection, SVG, verify, Office 2021, bundled-WASM, false-residual preservation, source-bound block/position add/remove, valid point-position override rejection, and `open-chestnut-basic` value/outside-end→category/top roundtrip cover the bounded slice. |
| `Workbook.create`, worksheets collection, `getRange`, values/formulas | done | Matrix writes and formula recalc implemented. `Workbook.create({ dateSystem: "1904" })` / `workbook.setDateSystem(...)` select Excel's 1900 or 1904 serial-date context, expose it through inspect/help, and roundtrip native `workbookPr@date1904`. |
| `SpreadsheetFile.exportXlsx` / `importXlsx` | partial | The JavaScript path roundtrips formulas, styles, validations, dynamic arrays, tables, pivots/caches, drawings, comments, worksheet views, native visibility, multiple workbook windows with independent active/selected tabs, and bounded calculation policy. The bundled OpenChestnut C# codec covers date systems, `calcPr`, visibility, bounded multi-window view graphs, sheets/cells, cached/shared/legacy/dynamic-array formulas, themes, static styles, merges/dimensions/views, ordinary range names, worksheet sorts, rich tables/filters/sorts, embedded PNG/JPEG worksheet pictures, and native bar/line/pie worksheet charts. `XlsxDrawingCodec` owns picture/ImagePart semantics while `XlsxChartCodec`, `XlsxChartSeriesStyleCodec`, `XlsxChartSeriesLineStyleCodec`, `XlsxChartSeriesMarkerCodec`, `XlsxChartLineOptionsCodec`, `XlsxChartDataLabelsCodec`, `XlsxChartTextStyleCodec`, and `XlsxChartAxisCodec` own GraphicFrame/ChartPart, per-series solid RGB fill, direct RGB line color/dash/width, marker symbol/size/RGB fill/bounded outline, chart-level standard/stacked/percent-stacked grouping, smooth interpolation, direct vary-colors, plot-level value/category data labels, title/tick-label font size, and primary-axis semantics; all safely share the worksheet DrawingPart using the same pre-mutation source hash. Worksheet chart field 14 carries one plot, title, legend, shared categories, bounded series values, optional category/value formulas, caches, one-cell/two-cell/absolute anchors, and a Drawing+Chart hash binding; series fields 5–7 carry RGB fill, direct line, and marker; marker fields 3/4 add its RGB fill and bounded outline; chart field 15 carries line options; chart field 16 carries data labels; chart field 14 carries optional title font size; axis field 7 carries optional tick-label font size; and chart fields 12/13 carry primary category/value axes. Source-free charts are authored through Open XML SDK relationships; recognized imports permit fixed-anchor/type/series/point/axis-topology name/title/legend/formula/cache/RGB-fill/direct-line/complete-bounded-marker/grouping/smooth/vary-colors/data-label/font-size/axis edits while residual ChartSpace XML and unrelated series shape properties survive. Formula-referenced series names, external data, combo/multi-plot charts, unrecognized series-fill/line/marker/grouping/smooth/vary-colors/data-label graphs, rich title runs, complex/conflicting axis `txPr`, axis-title styles, logarithmic/date/secondary/multi-axis profiles, complex/per-series/per-point labels, and unsupported drawing objects remain read-only, opaque, or fail closed. Source-free pictures retain all three anchors plus crop/effects/transform, and recognized imports permit fixed-topology metadata/geometry/effect/byte edits while preserving residual XML. Dynamic arrays reuse formula-metadata kind 3 and a canonical XLDAPR `CellMetadataPart`; unsupported metadata graphs fail closed. Workbook views require a complete hash-bound graph, worksheet identity topology is immutable, every export requires a visible sheet, and QueryTable/connection projections never expose or execute secret provider data. `open-chestnut-basic` crosses a formula-backed line chart with styled marker edits alongside series fill/line, grouping stacked→percentStacked, smooth true→false, vary-colors true→false, data labels value→category, title/tick-label font sizes, and primary axes plus three pictures through two WASM passes and real render QA. Needs broader third-party/Excel corpus validation, native pivots, richer charts/groups/shapes, and complex multi-record metadata profiles. |
| CSV/TSV import, export, inspect, and skill QA | done | `SpreadsheetFile.importDelimited/exportDelimited`, CSV/TSV convenience methods, and `inspectDelimited` implement bounded UTF-8 parsing/serialization with RFC-style quoted delimiters, escaped quotes, embedded newlines, BOM evidence, optional primitive coercion, calculated-value default export, explicit formula export, sheet/range selection, and byte/row/column limits. The runnable verifier accepts `.csv` and `.tsv`, emits bounded row evidence, and runs the same semantic/layout/visual gates; core and skill tests cover real CSV QA plus TSV roundtrip and malformed/oversized edge cases. Local macOS arm64 checks also pass both CSV and TSV through Playwright plus LibreOffice→PDF→Poppler native page verification. Delimited files intentionally flatten one worksheet and cannot preserve XLSX styling/drawings. |
| Formula calculation | partial | Supports arithmetic refs, defined names, Excel table sections/ranges/unions/space intersections, apostrophe-escaped special-character headers, unqualified calculated-column references, explicit `@`/`#This Row` row context, native shared/legacy/dynamic-array formula import/export, and workbook dependency graphs with dependents/cycles/missing-sheet errors. The JS codec and OpenChestnut preserve shared/legacy topology plus the bounded XLDAPR dynamic-array package graph: OpenChestnut carries expanded follower formulas and explicit anchor metadata, validates non-overlapping topology, preserves formula/metadata XML across safe edits, restores cached spill ownership before recalculation, and fails closed for blocked spills or unowned source metadata. Package prefixes `_xlfn.` and `_xlws.` are ignored only during evaluation, leaving the stored formula intact. Structured intersections return only cells common to every operand, support section and chained column overlap, and emit `#NULL!` when disjoint. Structured evaluation, graph, and trace share one dedicated syntax parser and expose only effective intersection precedents; metadata-free native XLSX roundtrips preserve the formula. The runnable `structured-intersection` fixture covers package/inspect/Playwright QA. The locally installed LibreOfficeDev 26.8 alpha displays `Err:509` for direct native execution but rewrites the expression to an A1-range intersection during ODS roundtrip; the engine accepts that rewrite and restores its exact value/precedents. Microsoft Excel execution remains pending Windows validation. Pivot calculated fields use a separate bounded AST over summed source fields. Its 45-function scalar whitelist covers 12 numeric helpers, `AND`/`OR`/`NOT`, lazy `IF`/`IFERROR`/`IFNA`, `NA`, `ISERROR`/`ISNUMBER`/`ISTEXT`, Excel Compatibility Version 2 surrogate-aware `LEN`/`LEFT`/`RIGHT`/`MID`, case conversion, ASCII-space `TRIM`, and workbook-date-system-aware `DATE`/`YEAR`/`MONTH`/`DAY`/`EDATE`/`EOMONTH`/`DAYS`/`WEEKDAY`/`TIME`/`HOUR`/`MINUTE`/`SECOND`/`NETWORKDAYS`/`WORKDAY`/`NETWORKDAYS.INTL`/`WORKDAY.INTL`; month shifts truncate their offset and clamp short months, while `WEEKDAY` supports return types 1, 2, 3, and 11 through 17. Business-day functions implement inclusive counts, reverse ranges, standard or Excel-compatible international weekend codes/patterns, and an optional scalar holiday; holiday arrays/ranges remain outside the bounded scalar AST. The evaluator retains the 1900 serial-60 leap-day quirk and 1,462-day 1900/1904 offset. It distinguishes literal `#`-prefixed text from actual Excel errors, validates unused branches, and forbids cell references or dynamic execution. Current-row references outside the table return `#VALUE!`. `COUNTIF(S)`, `SUMIF(S)`, and `AVERAGEIF(S)` share case-insensitive numeric/text comparison plus Excel `?`/`*`/`~` wildcard semantics; multi-range variants enforce equal dimensions and matched sum/average errors propagate. Dynamic arrays include selection, reshape, stack, and expansion helpers through `HSTACK`, `VSTACK`, and `EXPAND`. Lookup coverage includes `XMATCH` exact, next-smaller, next-larger, wildcard, forward/reverse, and sorted-search modes. Statistical/ranking coverage includes `MEDIAN`, `MODE.SNGL`, `LARGE`, `SMALL`, and `RANK.EQ`; signed decimal rounding includes `ROUND`, `ROUNDUP`, and `ROUNDDOWN`. Deterministic Excel 1900/1904 date arithmetic now includes `DATEVALUE`, `TIME`, `TIMEVALUE`, `HOUR`, `MINUTE`, and `SECOND` alongside `DATE`, `YEAR`, `MONTH`, `DAY`, `EDATE`, `EOMONTH`, `DAYS`, `WEEKDAY`, `NETWORKDAYS`, `WORKDAY`, `NETWORKDAYS.INTL`, and `WORKDAY.INTL`. The dedicated `src/spreadsheet/formula-coercion.mjs` accepts strict ISO and English month-name dates with four-digit years, 12/24-hour times, and `VALUE` text using ASCII grouping/scientific/percent/accounting syntax; ambiguous host-locale dates fail with `#VALUE!`. It retains the 1900 serial-60 quirk, 1,462-day system offset, 1904 serial zero, TIME overflow bounds/wrapping, invalid date/time-prefix validation, and date-system-specific range checks. Core tests cover invalid/ambiguous/overflow paths plus 1900/1904 XLSX roundtrip. The runnable formula-summary adds a third DateTimeText sheet and passes model, all-sheet Playwright, LibreOffice/Poppler, and native ODS→XLSX calculation gates. Error handling distinguishes `IFERROR`/`ISERROR` from #N/A-specific `IFNA`/`ISNA`, supports `ISERR`, and can emit `NA()`. The 88-entry formula help catalog spans math/statistical/logical/information/text/lookup/date-time/dynamic-array helpers. Needs broader configurable locale coercion, more array/coercion semantics, closer Excel error parity, and third-party dynamic-metadata corpus coverage. |
| `workbook.inspect` table/formula/match/sheet/workbook | partial | Core records implemented for workbook, source-bound connection roots, sheet (including gridline/frozen-pane view state, custom dimension counts, and merged-range counts), mergedCell, row/column dimensions, table/structured table, definedName, formula, formula graph, match, style/computedStyle, drawing/chart/image, sparkline, thread, dataValidation, and conditionalFormat with search, target, context-window, and include/exclude filtering. Connection records resolve by `connection/<id>` without exposing provider strings, commands, credentials, or source paths. Needs richer semantic context slicing and broader imported-object identity preservation. |
| `workbook.render` visual verification | partial | SVG grid preview and layout JSON consume native-equivalent per-column widths, per-row heights, hidden axes, merged-cell spans, number/date-formatted display values, symbolic theme/tint/indexed colors, patterned fills, extended font/alignment styles, table/chart/image/sparkline hints, conditional-format fills, sliced rules, gridline visibility, and frozen-pane state. The live `range.format` facade supports character/point and pixel sizing plus bounded deterministic `autofitColumns()`/`autofitRows()`. `range.fillDown()`/`fillRight()` copy contents and formatting while translating relative A1 formula references; merged intersections fail explicitly. With `--all-sheets true`, the runnable skill emits every worksheet's layout/Playwright preview and per-sheet baseline. `--native-render required` additionally gates the real XLSX through LibreOffice PDF and Poppler page PNGs, compares every native page, and rejects pagination changes; this path is exercised locally when tools are available. Needs full drawing fidelity, broader locale-specific number-format rendering, broader cross-platform evidence, and Windows Excel baselines. |
| `workbook.trace` | partial | Formula tracing now uses the same dependency model as recalculation, exposes precedent trees, graph nodes/edges via `workbook.formulaGraph()` and inspect kinds (`formulaNode`, `formulaEdge`, `formulaCycle`), resolves escaped/current-row/range/union/intersection table references through the shared parser, reduces intersections to their exact common-cell precedents, and reports cycles/missing-sheet refs to verify. Needs broader parser coverage and large-trace summarization. |
| Charts/tables/sparklines/images/comments/data validations/conditional formats | partial | Workbook Office 2019 threaded comments, validations/conditional formats, pivots, native tables, charts, images, sparklines, inspect/resolve/render hints, and consistency verification are implemented. The OpenChestnut path now authors/imports embedded PNG/JPEG pictures and bounded native bar/line/pie worksheet charts. Pictures support all three anchor kinds, crop, grayscale/luminance/opacity, rotation/flips, and safe same-format byte replacement. Charts support title/legend, literal or formula-backed shared categories, numeric caches, optional per-series solid RGB fills, direct RGB lines with five preset dash styles and bounded width, direct line-marker symbol/size/fill/outline, plot-level value/category labels, bounded title and primary-axis tick-label font sizes, primary category/value axis titles and number formats, category label intervals, value bounds/major units, fixed-topology source-bound edits, and exact residual ChartSpace retention; owned fills, lines, markers, data labels, and font sizes can be added, removed, or changed without replacing unowned series or text graphs, and images/charts can be edited together without invalidating their shared DrawingPart source binding. C#, JS, fallback, wire-presence, runnable-skill, Office 2021, and native-render tests cover the combined graph. Formula-referenced series names, unrecognized native fill/line graphs, rich/multi-run titles, complex/conflicting axis `txPr`, and logarithmic/native profiles import read-only and exact-roundtrip; chart add/remove, anchor/type/series/point/axis topology changes, external data, combo/multi-plot graphs, date/secondary axes, axis-title styles, and complex/per-series/per-point labels fail closed or remain opaque. Thread inspect records expose preserved native comment/person IDs and dates; verification rejects invalid modeled native metadata before export. Relationship-driven DrawingML and PivotTable behavior retains metadata-free roundtrip and runnable/native QA. Needs advanced picture effects, richer chart theme/axis styles and advanced labels, third-party profiles, grouped shapes, threaded-comment mentions/rich text, richer conditional formats, and broader Excel compatibility testing. |

## Presentations skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `Presentation.create`, slides collection, `slide.shapes.add` | done | Basic slide/shape/text facade works. |
| OpenChestnut PPTX codec | partial | The experimental `open-office-artifact-tool/codecs/open-chestnut` subpath authors/imports bounded shapes, rich text/field/break inlines, paragraph/list/default-run/body properties, markers, links, hash-bound Slide → Layout → Master identity, master `p:txStyles`, direct Master/Layout backgrounds, and owner-local Master/Layout placeholder text. `PptxBackgroundCodec` accepts only `p:bgPr/a:solidFill` or `p:bgRef`, preserves exact RGB or one of 16 theme tokens plus the reference index, and leaves effective inheritance in the JavaScript model. Missing direct backgrounds preserve source state; supported solid/reference backgrounds can be added or replaced. Direct removal, gradients, patterns, images, groups, effects, and transforms remain source-bound and fail closed. `PptxPlaceholderCodec` exposes direct `p:ph` identity plus local `a:txBody`, binds every entry to its owner and raw shape-tree position, and permits fixed paragraph/inline-topology text/body/list/link edits while leaving geometry, fill, shape style, inherited/effective formatting, name, type, and index in the source element. `PptxPartContext` keeps relationships local to their Slide, Master, or Layout owner. Source export rejects graph, owner/identity, placeholder-topology, layout-content, and slide-layout rebinding changes, validates Office 2021 output, compares the guarded OPC graph, and residual/byte-checks all unmodeled content. The runnable fixture changes direct master/layout backgrounds, edits both template owners' placeholder text including a Layout-owned hyperlink, edits master styles and shape text/body/list content, then passes bundled-WASM, Playwright, LibreOffice, and Poppler locally. Hosted Linux run [`29298306670`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29298306670) passes the same complete gate with 34 C# tests, the reproducible 38-file/12,864,199-byte runtime, and the 192-file clean-install package. Placeholder geometry/fill/shape-style and identity edits, direct background removal, slide rebinding, transformed/other colors, custom-show links, WordArt/compatibility properties, chart editing, broader corpus evidence, and Windows PowerPoint validation remain pending. |
| `PresentationFile.exportPptx` / `importPptx` | partial | PPTX roundtrip covers direct and grouped DrawingML shapes, connectors, tables, charts, pictures, notes, comments, themes, layouts, rich text paragraphs with relationship-backed and action-only hyperlinks, and source-bound opaque native objects whose recognized top-level OLE/diagram/contentPart-group profiles permit only name and outer-frame edits. `shape.text.set(...)` accepts ordered paragraphs and styled runs, Unicode or relationship-backed picture bullets, standard DrawingML auto-number schemes, marker font/color/fixed-or-percentage size, explicit follow-text marker overrides, levels, alignment, indents, point/percent spacing, and run formatting; inspect/layout/SVG and native export share the normalized model. Structured runs accept one non-fetched absolute external URI, internal slide ID, or relationship-free slide-show action (`nextSlide`, `previousSlide`, `firstSlide`, `lastSlide`, `endShow`) plus optional tooltip, target frame, history, and click-highlight state. The dedicated `src/presentation/ooxml-picture-bullets.mjs` planner now owns direct/grouped, Slide Master `p:txStyles`, master-placeholder, and Slide Layout placeholder picture bullets. Embedded PNG/JPEG/GIF/SVG data URLs share media bytes across owners while each owning OOXML part receives its own collision-free image relationship; external URIs remain non-fetched with `TargetMode="External"`. Metadata-free import follows the owner part's `.rels`, rejects missing/wrong-type/missing-part targets, accepts alternate relationship prefixes and strict OOXML image relationship URIs, restores editable inherited styles, honors direct marker overrides, and preserves them through second export. Core and runnable-skill fixtures cover master/layout/slide ownership and inherited placeholders; standalone authored and second-export packages both pass Open XML SDK Office 2021 with zero errors. Local LibreOfficeDev 26.8 opens and renders the package but remains inconsistent: master-placeholder and layout-placeholder image markers are visible while the Slide Master `p:txStyles` marker is omitted; Windows PowerPoint visual validation remains pending. Placeholder geometry and other paragraph properties cascade through Slide Master `p:txStyles` title/body/other levels, master placeholders, layouts, and slides. The dedicated `src/presentation/ooxml-charts.mjs` codec owns bounded standard style IDs (`styleId`/`styleIndex`), color variation, horizontal/vertical bar direction, clustered/stacked/percentage grouping, gap/overlap, series fill plus bounded line width/dash styling, indexed `c:dPt` fill/line overrides, chart/per-series line markers and smoothing, primary/secondary-axis bar, line, and bar+line combo plot groups, chart/per-series value/category label visibility and positions, and series trendlines. Label overrides accept long public aliases or native OOXML positions, can explicitly disable inherited chart labels, feed inspect/layout/SVG, accept alternate prefixes, and survive metadata-free second export. `trendline`/`trendlines` accept all six standard DrawingML types with public aliases, type-specific bounded polynomial order or moving-average period, bounded half-category forward/backward extension, optional intercept/name/equation/R-squared flags, and line styling; all types feed inspect/layout/native OOXML and metadata-free second export. The dedicated `src/presentation/chart-trendline-svg.mjs` now previews linear, exponential, logarithmic, moving-average, polynomial, and power fits with bounded deterministic sampling on vertical/horizontal bars, lines, combo plots, and primary/secondary scales; it omits invalid positive-domain fits instead of emitting non-finite SVG. Bar/line series may split one plot type across primary and secondary OOXML plot blocks; combo series require an explicit bar/line type, retain global series order across all plot groups, preserve both option sets plus axis ownership, expose mixed SVG/layout/inspect output, accept alternate prefixes, and survive metadata-free second export. Independent authored and metadata-free second-export combo, label-override, trendline, secondary-axis, and same-type split-axis decks pass Open XML SDK Office 2021 with zero errors; the runnable `agent-readiness` fixture passes model/package gates and visibly renders columns plus primary- and secondary-axis line groups, markers, category/value labels, styled linear and polynomial trendlines, error bars, and legend through LibreOffice/Poppler. Authored and python-pptx point/series-format decks pass Open XML SDK Office 2021 with zero errors and visibly retain dashed lines, overridden fills, and point borders through LibreOffice/Poppler. Metadata-free text import likewise accepts alternate prefixes and preserves third-party master-only character bullets, marker fonts, nested margins, font sizes, and percentage spacing through second export. Fixed Georgia/red/150% and follow-text auto-number fixtures plus a python-pptx `•`/`–`/`•` title/content deck pass Open XML SDK Office 2021 validation with zero errors and render correctly through LibreOffice/Poppler. The dedicated `src/presentation/ooxml-hyperlinks.mjs` planner owns collision-free external-hyperlink and internal-slide relationships plus relationship-free slide-show actions for direct/grouped shapes, Slide Master, and Slide Layout placeholders. It emits `a:hlinkClick`, maps the five public actions to standard `ppaction://hlinkshowjump` URIs with empty `r:id`, allocates no relationship for them, de-duplicates repeated relationship targets per owner, resolves metadata-free internal targets back to current model slide IDs, accepts alternate namespace prefixes and strict relationship URIs, rejects unsupported action URIs and missing/wrong-mode/wrong-type/missing-slide relationships, and preserves all supported links through second export. Core tests cover all owners and all five actions; a dedicated all-five-action authored and metadata-free second-export deck passes Open XML SDK Office 2021 with zero errors plus Playwright and LibreOffice/Poppler one-page render gates, while the runnable `agent-readiness` fixture exercises external URI plus `nextSlide` model/package/SVG and LibreOffice/Poppler QA. Unsupported top-level or grouped `p:contentPart` and `p:graphicFrame` children become agent-visible `nativeObject` facades with recursively preserved OPC graphs; recognized top-level OLE, SmartArt/diagram, and contentPart-group profiles expose only `setName(...)` and `setPosition(...)`, while generic/nested/incomplete profiles remain read-only; real python-pptx OLE and Open XML SDK SmartArt assets also pass validation after second export. Native-object payload semantic editing, content-part-specific preview rendering, grouped-object placement projection, and broader third-party PPTX fidelity remain gaps; unsupported modern-comment targets still use `unknownAnchor`. |
| Presentation custom shows and run links | done | `presentation.customShows.add(...)` creates bounded named ordered slide sequences with stable facades and collision-free preserved unsigned native IDs. The dedicated `src/presentation/ooxml-custom-shows.mjs` codec writes schema-ordered `p:custShowLst`, reuses presentation-to-slide relationships, accepts alternate namespace prefixes, rejects duplicate names/IDs and missing/wrong-mode/wrong-type slide relationships, and restores editable shows through metadata-free second export. Structured runs target a show by name with optional `returnToSlide`; the hyperlink codec resolves the native show ID into relationship-free `ppaction://customshow` markup with empty `r:id`, rejects dangling definitions, exposes SVG evidence, and preserves master/layout/direct/grouped links. Core tests cover malformed graphs and all owners; a dedicated authored/second-export deck passes Open XML SDK Office 2021 with zero errors and Playwright plus LibreOffice/Poppler 3/3 render gates, while the runnable `agent-readiness` fixture covers a returning two-slide show. |
| `presentation.inspect` slide/textbox/shape/layout | partial | Stable records implemented for deck, theme, all Slide Masters, layout templates, custom shows, slides, textboxes, text ranges, shapes, grouped-shape trees, tables, charts, images, connectors, bounded native content-part/OLE/diagram objects, notes, comments/threads, and layout, with shared search/target/context/include/exclude filtering. Native records expose classification, source part, relationship/part counts, geometry, exact editable fields, and safe OLE workbook path/content-type/byte-count/source-digest metadata when eligible. Needs richer target-specific semantic slices. |
| Presentation chart error bars | partial | Series `errorBars` supports x/y direction, both/minus/plus extent, fixed value, percentage, standard deviation, standard error, and per-point custom plus/minus arrays with strict series-length validation, end-cap control, and bounded line styling. Custom sources accept bounded `plusFormula`/`minusFormula` references, aliases, optional caches and format codes, and require relationship-backed embedded XLSX bytes or an absolute non-fetched external workbook URI. Embedded bytes pass the XLSX package inspector before export. The dedicated chart/data codecs emit schema-ordered `c:errBars` with `c:numLit` or `c:numRef`/`c:f`/`c:numCache`, `c:externalData`, chart relationships, content types, and embedded workbook parts; import accepts alternate namespace prefixes, preserves formula-only references plus embedded bytes or external URIs, and survives metadata-free second export. Package validation rejects missing/wrong-type/orphaned relationships and invalid workbook content types. SVG previews scalar and cached custom error bars on line, column, and horizontal-bar series. Authored and second-export formula-reference decks pass Open XML SDK Office 2021 with zero errors and visibly render through LibreOffice/Poppler; the runnable `agent-readiness` fixture builds its embedded workbook through the public spreadsheet API and passes model/package/native gates. Broader Windows PowerPoint evidence remains pending. |
| Presentation chart secondary axes | partial | Bar, line, and combo-chart series accept `axisGroup: "secondary"` plus bounded `y2`/boolean aliases while retaining at least one primary series. Export partitions each bar/line type into primary and secondary OOXML plot blocks, emits one category/value pair per used axis group with unique IDs, top/right positions, symmetric `crossAx` references, and `crosses="max"`, and preserves global `c:order` values across up to four combo groups. Import traverses every repeated `c:barChart`/`c:lineChart` block, identifies ownership from its axis IDs and positions, accepts alternate namespace prefixes, restores secondary titles, and preserves global series order through metadata-free second export. Inspect/layout expose axis ownership; SVG computes stacked totals and maxima per axis group and draws a visible right-side axis. Core tests cover bar/line/combo splits plus invalid groups, pie rejection, missing primary series, and configured-but-unused axes. Authored and second-export same-type line and bar decks pass Open XML SDK Office 2021 with zero errors and visibly render independent left/right scales through LibreOffice/Poppler; the runnable fixture exercises primary-column plus primary/secondary-line groups through Playwright and native 3/3-slide QA. Windows PowerPoint evidence remains pending. |
| `presentation.resolve` | partial | Resolves presentations, themes, all Slide Masters, slide layouts, slides, shapes, nested grouped shapes/children/text ranges, tables, charts, images, connectors, native content-part/OLE/diagram facades, and comment threads. Native payloads remain read-only except for the explicit source-bound OLE XLSX getter/replacement contract. |
| `slide.export({format:'layout'})` | done | Layout JSON covers shapes, grouped-shape trees with local and absolute frames, textboxes, tables, charts, images, connectors, and native-object placeholders including safe eligible embedded-workbook summaries, with target/search/context slicing over stable element, textRange, native-object, and comment IDs. |
| `presentation.export` image preview/montage | partial | SVG preview covers modeled elements and deck montage thumbnails. The Presentations skill renders every modeled slide with Playwright, creates a montage, gates the real PPTX through LibreOffice PDF + Poppler per-slide PNGs, compares model/native baselines, cleans stale slide baselines when approving updates, and rejects slide-count changes. Needs richer DrawingML fidelity and broader cross-platform baseline evidence. |
| Compose/JSX layout + token parser | partial | Helper-node compose engine implemented for row, column, grid, layers, box, paragraph, run, shape, table, chart, image, and rule with fill/hug/fixed sizing, fr/fixed grid tracks, spans, gaps, padding, stable names/ids, text class tokens, inspect, resolve, layout JSON, and PPTX roundtrip. `slide.autoLayout` now places existing shapes with horizontal/vertical flow, frame, gap, padding, and alignment. Package exports now include `./presentation-jsx`, `./presentation-jsx/jsx-runtime`, and `./presentation-jsx/jsx-dev-runtime` with `jsx`, `jsxs`, `jsxDEV`, `Fragment`, helper nodes, and function component support. Table nodes emit native PPTX DrawingML tables; chart nodes emit native PPTX bar/line/pie and primary/secondary-axis bar/line and bar+line combo chart parts with repeated same-type plot groups with title/category/series/color/axis/legend/data-label/trendline/error-bar metadata plus standard style, bar grouping/direction/gap/overlap, series/point fill and line styling, and line marker/smoothing options; and `dataUrl` images emit native PPTX image parts. Scalar, literal-custom, and formula-reference error bars preserve native OOXML/data parts and render from cached values in SVG. Needs fuller token parser and richer label styling. |
| Overlap/overflow/template fidelity QA | partial | `slide.validateLayout()` and `presentation.validateLayout()` detect off-canvas elements, connector endpoints outside the slide, geometry overlaps, basic text overflow, and table cell overflow; `presentation.verify()` also detects dangling comment targets, required placeholder/template gaps, chart/category/series mismatches, non-numeric chart data, table shape issues, and empty/invalid image data. The runnable skill adds full-size per-slide model/native review and optional baseline diffs. Needs richer placeholder/template fidelity gates and Windows PowerPoint evidence. |

## Documents skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| OpenChestnut DOCX numbering authoring and definition edits | partial | `DocxDirectNumbering` preflights the complete source-free document, groups direct instances and compatible shared abstract definitions, and emits schema-ordered text-marker levels 0–8 plus paragraph assignments. The JS adapter allocates package-local IDs when omitted and both sides reject conflicting instance/abstract/level definitions before package mutation. After import, `DocxNumberingEditPlanner` permits `numberFormat`, `start`, and `levelText` changes only when all editable top-level paragraphs directly assigned to one `numId`/level request the same target. It writes or updates an instance-local `w:lvlOverride`, preserving the shared `w:abstractNum`, paragraph assignment, formatting residual, and opaque package graph. Partial groups, nested uses, another part's references, style inheritance/linkage, identity/level changes, and incompatible override topology fail closed. The runnable `open-chestnut-numbering-edit` fixture now authors, imports, edits, and reimports entirely through bundled WASM and passes Playwright plus LibreOffice/Poppler locally. Picture-marker/style-linked source-free graphs, nested/linked graph edits, and Windows Word validation remain pending. |
| OpenChestnut DOCX table geometry and formatting | partial | Source-free OpenChestnut authoring validates a complete physical-cell graph and emits schema-ordered `tblGrid`, `gridSpan`, and `vMerge`. The additive optional `DocumentTableFormatting` wire profile adds exact dxa table width/indent, one width per logical grid column, derived merged-cell widths, four margins, fixed layout, six uniform direct RGB borders, and first-row fill/bold. Both adapters require widths to sum exactly, bound dimensions/colors/border sizes, and fail closed before mutation. Reimport recognizes only the complete direct profile; recognized width/indent/grid/cell-width/margin/border/header-fill fields can then be updated in place while the residual hash canonicalizes exactly those modeled values and continues to protect style, row, paragraph, run, and other unknown XML. Missing, partial, theme, conditional, per-cell, or style-effective formatting remains source-preserved and rejects replacement. The runnable `open-chestnut-merged-table` fixture authors geometry and non-default formatting through bundled WASM, reimports, edits both the merge origin and formatting, and passes package/model plus real Playwright/LibreOffice/Poppler locally. Rich cell contents, broader direct/effective formatting, and Windows Word evidence remain pending. |
| `DocumentModel.create`, paragraphs, lists, headers/footers, hyperlinks, bookmarks, fields, citations, images, sections, tables, comments, styles, `inspect` | partial | Block model now supports an agent-facing Word theme, document-wide default run properties, paragraph and character styles with `basedOn` chains, `runStyleId` character-style references, styled paragraph spans with source-plus-resolved values, multi-level list items, headers/footers, external and internal hyperlinks, single- and multi-block bookmark ranges whose endpoints may be blocks or exact `table.getCell(row, column)` facades, native-tagged citations plus independent bibliography sources, fields, images, sections, tracked changes, tables, comments, textRange/tableCell anchors, inspect/resolve/search, page-aware layout, SVG preview, and help entries. `document.addBibliographySource(...)` models Word source types, personal/corporate authors, publication fields, source tags and style metadata; citations automatically create or reuse their tagged source. Effective run formatting uses Word precedence—document defaults, paragraph chain, character chain, direct formatting—and feeds layout/SVG; explicit false values and direct/theme conflicts override correctly. Settings cover revision tracking, field refresh, even/odd headers, mirrored margins, and passwordless editing restrictions. Per-section settings preserve different-first-page activation separately from first-page references; layout pages expose selected default/first/even IDs, source section, inherited/blank state, and page-in-section. Needs richer pagination and text measurement, especially decoration changes inside a continuous section page. |
| `DocumentFile.exportDocx` / `importDocx` | partial | WordprocessingML round trip writes relationship-backed Theme, settings, styles, classic comments, Office 2013 `commentsExtended`, Office 2019 `commentsIds`, Office 2021 `commentsExtensible`, Word `people`, numbering, header/footer, external/internal links, bookmark ranges, fields, native `CITATION` fields plus `b:Sources` custom XML, images, sections, tracked changes, lists, and tables. The dedicated `src/ooxml/docx-bibliography.mjs` codec validates all 17 public Word source types and bounded source/style fields, serializes personal/corporate authors with deterministic GUIDs, follows arbitrary `customXml` relationship targets, accepts alternate namespace prefixes, restores cited and uncited sources plus field results through metadata-free import/second export, and rejects duplicate/invalid/unlinked sources or dangling citation tags at the package boundary. The dedicated comments, links, sections, and numbering codecs retain their documented durable identity, exact block/table-cell range, section inheritance, and list-style behavior. The runnable business-brief fixture exercises native citations/sources, style-linked multilevel numbering, picture bullets, multi-block/table-cell bookmarks, section inheritance, durable comments, package/model/Open XML SDK, and native-render QA. Needs richer pagination and arbitrary Word corpus evidence. |
| OpenChestnut DOCX migration slice | partial | The bundled C# Open XML SDK codec imports/exports ordered paragraphs, limited direct run formatting, table text/merge geometry/fixed direct formatting, bounded whole-paragraph hyperlinks, one-result-run `w:fldSimple` fields, and direct or paragraph/numbering-style-linked numbered single-run paragraphs. Dedicated codecs own hyperlink relationships, bounded fields, complete source-free `tblGrid`/`gridSpan`/`vMerge` authoring, fixed width/grid/margin/border/header formatting plus exact-profile imported edits, merge-aware imported table text, source-free direct text-marker numbering graphs, numbering resolution, and coherent direct numbering-definition group edits. Imported blocks carry body position, source-element SHA-256, semantic SHA-256, residual SHA-256 where applicable, and editability. Unmodeled topology and package content remain source-preserved and fail closed. The `business-brief`, `open-chestnut-merged-table`, and `open-chestnut-numbering-edit` fixtures pass model/package plus real Playwright/LibreOffice/Poppler QA. Local gates pass 56 C# tests, the 209-file no-local-dotnet clean install, and two reproducible 39-file build snapshots around a 38-file/12,981,948-byte macOS runtime. Hosted Linux runs [`29343501551`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29343501551) and [`29343983983`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29343983983) pass the same 56-test/209-file gate, OfficeBridge 5/5, real Chromium/LibreOffice/Poppler, and two byte-identical 38-file/12,981,950-byte runtime builds. Comments/bookmarks, rich table-cell content, theme/conditional/per-cell formatting, drawings, sections, picture/style-linked direct numbering graphs, broader third-party Word corpus, and Windows Word validation remain pending. |
| DOCX numbering-style inheritance | done | Paragraph and numbering styles preserve `pPr/numPr` identities through `numberingId`/`numberingLevel`; style-only paragraphs resolve `basedOn` cascades, use level `pStyle` rather than the ignored style `ilvl`, and honor direct `numId=0` cancellation. The numbering codec resolves `numStyleLink` through the referenced numbering style and underlying instance/abstract definition, retains `styleLink` identity, applies instance overrides after link resolution, accepts alternate prefixes, and rejects duplicate definitions, missing/wrong-type styles, missing instances, invalid levels, and cycles. Second export safely flattens linked definitions into schema-ordered native levels while retaining paragraph and numbering style links. The business-brief fixture authors linked level styles through the public facade and passes metadata-free second import plus native render QA. |
| DOCX picture bullets | done | `document.addListItem(..., { pictureBullet })` accepts bounded embedded PNG/JPEG/GIF data URLs or non-fetched absolute http(s) URIs with point dimensions and alt text. The dedicated `src/ooxml/docx-numbering.mjs` codec now owns list grouping, full source/dimension/alt conflict validation, schema-ordered `w:numPicBullet`/`w:abstractNum`/`w:num`, `w:lvlPicBulletId`, Numbering-part image relationships/media, VML export for Word/LibreOffice compatibility, and both VML plus DrawingML import. Distinct presentations of one image retain separate bullet definitions while sharing one media relationship. Metadata-free import follows arbitrary Numbering part and `.rels` paths, accepts alternate namespace prefixes, rejects missing/wrong-type/missing-part relationships, and preserves picture metadata through second export. The runnable business brief carries a visible green marker through model SVG and LibreOffice/Poppler; an isolated authored DOCX passes Open XML SDK Office 2021 with zero errors and one-page Playwright/native QA. Windows Word visual evidence remains pending. |
| Design presets / table geometry / OOXML patch helpers | partial | `document.applyDesignPreset()` and inherited effective styles feed inspect/layout/render/export. `DocumentFile.inspectDocx()` and `patchDocx()` use the shared bounded, path-validated, atomically result-validated OOXML package layer. Settings, header/footer, classic comments, `commentsExtended`, `commentsIds`, `commentsExtensible`, `people`, and numbering recipes retain arbitrary-target relationship and semantic-validation behavior. Runnable `business-brief`, `package-settings`, `package-comments`, and `package-numbering` fixtures restore native style cascades, theme/run semantics, settings, comment identities/targets, people metadata, and multilevel list metadata with `{ preferNative:true }`, then pass LibreOffice→PDF→Poppler. The generated business brief also passes Open XML SDK Office 2021 validation with zero errors after schema-ordering table properties. `document.verify()` covers unknown paragraph/character styles, invalid links/citations, section indexes, malformed images/sections/tables, comment identity/person errors, and modeled overflow. Needs additional settings such as write protection/proof state only when agent workflows demonstrate demand. |
| DOCX render-to-page images | partial | `document.render()` provides a clean-room SVG/page-layout preview and `renderArtifact(..., { source: 'docx', renderer })` can feed the real DOCX into LibreOffice/native Office adapters. The runnable Documents skill now creates and compares a Playwright model PNG baseline plus every LibreOffice→PDF→Poppler native page PNG, records per-page pixel metrics, rejects changed page counts, and exercises this flow locally when the installed native tools are available. Needs richer pagination fidelity, broader cross-platform baseline evidence, and Windows Word comparison. |

## PDF skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `PdfArtifact.create`, `PdfFile.exportPdf`, `PdfFile.importPdf` | partial | Multi-page modeled PDFs support positioned text, tables, images/charts, stable IDs, and real PDF 1.7 export with PNG/JPEG XObjects. The dedicated `src/pdf/table-grid.mjs` normalizes table origin/covered cells, stable cell IDs, zero-based `cells` overrides, editable `table.getCell(row, column)` facades, row/column spans, `TH`/`TD` roles, `Row`/`Column`/`Both` scopes, explicit/inferred header associations, cell bounding boxes, and overlap/range/header validation. SVG, layout JSON, inspect/resolve/extract, vector PDF drawing, and the tagged structure tree consume that same grid. Export is tagged by default with Catalog language, MarkInfo, StructTreeRoot/ParentTree, per-page StructParents, cell-level MCIDs and IDs, H1-H6/P/Figure elements, semantic `Table` → `TR` → `TH`/`TD` structure, and standard Table-owner `RowSpan`/`ColSpan`/`Scope`/`Headers` attributes. `inspectPdf()` reports top-level reading-order IDs plus H1-H6, cell-ID, span, header-association, tagged/language/marked-content, font, and per-role evidence. The runnable fixture includes a two-level column header with both row and column spans plus an explicit association; its structural gate derives expected counts from normalized origin cells. Optional standalone glyf-based TrueType input produces embedded Type0/CIDFontType2 objects, Identity-H CID encoding, explicit CIDToGID mapping, widths and a `ToUnicode` CMap. Default deterministic subsetting preserves used glyphs plus recursive composite dependencies and rebuilds `glyf`/long-`loca`/format-12 `cmap`, sfnt checksums and `checkSumAdjustment`; `{ subsetFont:false }` retains diagnostic full-font output. Unsafe fallback replacement is forbidden, and malformed/oversized fonts, `.ttc` collections and missing glyphs hard-error. Public Liberation Sans tests reduce a Unicode fixture from about 93 KB full-font to 16 KB subset; local Chinese validation reduces about 15.2 MB to 70 KB while PDF.js extraction and Poppler pixels remain correct. Import supports full modeled-span and reading-order metadata roundtrip, injected parsers, geometry-based simple-table reconstruction, and PDF.js. Needs complex-script shaping, metadata-free spanning-cell reconstruction from arbitrary PDFs, arbitrary-PDF corpus evidence, formal PDF/UA validation, and broader device-independent color-space/profile handling. |
| Explicit PDF logical reading order | done | `page.setReadingOrder(...)` and page `readingOrder` config accept stable IDs or facades for non-positioned body text, positioned text, tables, images, and charts. The dedicated `src/pdf/reading-order.mjs` owns body-text identity, normalization, exact-coverage/duplicate/unknown/ambiguity validation, inspect/layout records, and bounded extraction of top-level structure IDs from real PDF bytes. Tagged export reorders `StructTreeRoot /K` while retaining the original content-stream paint sequence and MCID-indexed ParentTree; incomplete declarations fail verification and export. Model metadata roundtrip preserves the declaration. The runnable fixture intentionally differs logical and visual order, compares modeled IDs with the real structure tree, and rejects a tampered PDF before PDF.js/Playwright/Poppler delivery gates. This is structural evidence, not full PDF/UA conformance. |
| PDF Figure alt text / decorative artifacts | done | Informative images and charts preserve explicit `alt` text and fail `pdf.verify()` when it is missing or generic; `decorative:true` content remains visually painted but is excluded from logical reading order and emitted as `/Artifact BMC` rather than a `Figure` structure element. The dedicated `src/pdf/accessibility.mjs` owns normalization, semantic QA, and bounded real-byte Figure `/Alt` plus Artifact inspection. The runnable workflow derives modeled expectations from the artifact, compares modeled/file counts, and rejects a tampered missing-Alt structure before PDF.js/Playwright/Poppler gates. Formal PDF/UA validation remains separate. |
| PDF semantic heading hierarchy | done | Positioned text accepts `headingLevel:1..6`, preserves it through inspect/layout/model import/export, and emits matching H1-H6 marked content and structure roles independently of visual styling. `pdf.verify()` rejects sequences that start below H1 or skip a level in logical reading order. `inspectPdf()` exposes total/per-level counts, and the runnable workflow compares modeled/file roles and rejects a flattened H2 before PDF.js/Playwright/Poppler delivery gates. Formal PDF/UA validation remains separate. |
| PDF visual render verification | partial | SVG page render draws modeled text, positioned text items, tables, image regions, and charts; layout JSON is page-sliced. The PDF skill renders every modeled page with Playwright and every real PDF page with Poppler, verifies page counts with `pdfinfo`, compares model/native PNG baselines, cleans stale baselines on approval, and rejects baseline page-count changes. The two-page fixture passes this full loop on macOS arm64. Needs broader cross-platform and arbitrary-PDF baseline evidence. |
| Text/table extraction | partial | `extractText()` and `extractTables()` work for modeled/generated PDFs; extracted modeled tables include normalized origin cells, spans, roles, scopes, explicit/effective header IDs, and cell bounding boxes. Import detects pipe-delimited/aligned tables and reconstructs simple geometry tables. PDF.js parses real bytes into text items, simple tables, actual bounded PNG image data when raw XObject pixels are available, and fill-colored RGBA PNGs for 1-bit stencil/solid masks; the runnable skill records real-byte parsing alongside Poppler evidence. Needs richer multi-line and metadata-free spanning-cell reconstruction plus complex device-independent color-space conversion. |
| Create polished reports with typography/layout/charts | partial | Modeled creation/export supports positioned text, span-aware vector tables with multi-level accessible headers, PNG/JPEG images, contain/cover placement, clipping, explicit logical reading order, Figure alt/decorative semantics, a tagged-PDF accessibility baseline with `Table`/`TR`/`TH`/`TD` hierarchy and standard table attributes, and caller-supplied subsetted Unicode TrueType embedding. `pdf.addFlowText(...)` estimates public Helvetica metrics, wraps paragraphs and long tokens inside margins, continues below existing text, appends same-size pages automatically, and preserves stable flow metadata. The runnable fixture forces a third page and requires tagged/language/MCID/structure/reading-order/Figure/Artifact/span/header-association evidence alongside PDF.js, Playwright, Poppler, page-set baselines, and package objects; `--font` enables Unicode fixture creation and `--subset-font false` enables diagnostic full-font comparison. Needs complex-script shaping, richer typography/chart styling, color profiles, and formal PDF/UA validation. |

## Core Office OpenChestnut codec target

The evidence and language split behind this target are recorded in [`docs/reference-runtime-architecture.md`](reference-runtime-architecture.md).

| Requirement | Status | Notes |
| --- | --- | --- |
| PPTX native-object graph projection | done | Additive `PresentationOpaqueElement` fields 8–10 carry a forward-compatible `native_kind`, namespace-qualified owner-local relationship references, and the bounded transitive part-path closure; `PresentationNativeRelationshipReference` is independent public metadata. `PptxNativeObjectCatalog` classifies top-level or nested `contentPart`, OLE, SmartArt/diagram, pictures, groups, connectors, and generic graphic frames through Open XML DOM namespaces, resolves relationships against the canonical opaque inventory, validates every internal target before the SDK loads semantic parts, and traverses cycles under `max_cells`/`max_parts` budgets. No bytes or relationship metadata are duplicated outside `opaque_opc`. The async JS adapter verifies part SHA-256, materializes only referenced source-snapshot entries, populates ordinary `nativeObject` facades, and exposes safe reference/relationship/part summaries plus exact editability fields through inspect while resolve returns the full facade. Only recognized top-level OLE/diagram GraphicFrames and contentPart-bearing GroupShapes with complete names/transforms become placement-editable; their public setters own name and outer frame while raw XML residuals, OLE payloads, nested group coordinates, relationships, content types, part bytes, and topology remain immutable. C# tests cover Office 2021-valid OLE/diagram/contentPart graphs, recursive custom XML properties, bounded name/frame edits, wire-editability escalation, raw-XML/frame tampering, cycles, missing relationships/parts, budgets, source-bound edits, and exact payload-byte preservation. Bundled-WASM tests also cross the result through the JavaScript fallback exporter. The runnable presentation fixture injects all three native kinds, asserts 2/4/2 part closures and 2/4/1 root relationships, renames and moves/resizes every recognized object, re-imports their updated frames while requiring exact payload bytes, and runs package/semantic/Playwright/LibreOffice/Poppler QA. The JavaScript fallback exporter applies the same bounded placement rewrite. Payload-semantic editing of these native objects, dedicated previews, richer grouped-object projection, and broader third-party/Windows PowerPoint corpus evidence remain pending. |
| PPTX OLE embedded-workbook replacement | done | Additive `PresentationOpaqueElement.ole_workbook` field 11 carries an independently designed `PresentationOleWorkbook` locator: immutable part path, XLSX content type, source SHA-256, owner-local package relationship ID, and an export-only replacement asset ID. The capability is exposed only for a top-level placement-recognized OLE with exactly one internal `/package` relationship to one non-shared ordinary XLSX part. Both codecs count inbound relationships across the complete OPC package, so duplicate owner-local relationships and targets shared across slides fail closed. `getEmbeddedWorkbook()` returns a defensive `FileBlob`; `replaceEmbeddedWorkbook(...)` changes only those bytes. OpenChestnut validates the replacement under OPC budgets, requires a workbook/worksheet graph, runs Open XML SDK Office 2021 validation, writes the existing `EmbeddedPackagePart`, and then proves requested SHA-256 plus unchanged content type/relationships/topology. The JavaScript fallback independently detects the same relationship profile, deep-inspects changed XLSX bytes, and rejects mutation of every other source-hashed native part. Preview image bytes are intentionally preserved and may be stale. C#/bundled-WASM/JS-fallback tests and the runnable preservation fixture cross real source/replacement workbooks while preserving the preview, SmartArt, contentPart graph, raw XML, and all unrelated bytes. OLE creation, shared package targets, non-XLSX OLE payloads, preview regeneration, arbitrary part replacement, and semantic projection of the embedded workbook into the parent presentation remain unsupported. |
| Versioned public Office artifact wire schema | partial | The independently designed v1 protobuf sources and generated JS/C# bindings cover protocol/versioning, content-addressed assets, diagnostics, source identity, opaque OPC preservation, workbook/document semantics, and loss-aware presentation objects. `DocumentBlock` adds hyperlink content at field 13 and simple-field content at field 14 without changing paragraph/table/opaque tags; `DocumentParagraph` additively exposes direct/resolved `DocumentNumbering` at field 3, whose additive field 7 carries resolved `styleLink`/`numStyleLink` numbering-style identity without changing fields 1–6. `DocumentTable` retains rows at field 1, logical grid width at field 2, and adds optional direct formatting at field 3. `DocumentTableFormatting` independently carries fixed dxa table/indent/grid widths, four margins, uniform RGB border size/color, and header fill; message presence distinguishes a complete modeled profile from arbitrary source-preserved formatting. Rows retain the physical text matrix at field 1 while adding source-bound rich cells and row offsets at fields 2–4. Each new rich cell exposes grid column, column/row span, vertical-merge state, and editability without replacing legacy text values. `DocumentHyperlink` independently models external URI or internal anchor plus source-locator relationship ID and optional tooltip/history, `DocumentField` models instruction/display, and `DocumentSourceBinding` adds residual SHA-256 at field 5. `PresentationArtifact` retains slides at field 5 and additively exposes masters/layouts at fields 6/7; slides add layout locator field 5. Master/Layout messages add direct background fields 5/6 without changing existing identity or text-style tags. `PresentationBackground` independently represents RGB/theme color and solid/style-reference mode; master source bindings add background semantic hash/editability fields 7/8 and layout bindings add fields 5/6. These fields represent direct `p:bg`, never a computed inherited fill. Master bindings retain the `p:txStyles` semantic hash/editability contract and explicit title/body/other deleted-level lists. `PresentationOpaqueElement` additively uses field 11 for the bounded OLE workbook locator/replacement contract without changing fields 1–10. Package relationship IDs remain package locators rather than asset identity or cross-import durable IDs. No private reference schema, field numbers, or generated code are used. |
| Opaque OPC part graph and content-type boundary | done | `PackageGuards` parses the namespace-qualified `[Content_Types].xml` once for DOCX/XLSX/PPTX, resolves every OPC part through an exact override or extension default, and rejects missing manifests, malformed XML, duplicate case-insensitive defaults/overrides, unsafe override paths, invalid media types, and parts without a declaration. The manifest itself is correctly excluded from self-typing. Every unowned `OpaqueOpcPart` now carries its exact resolved `content_type`, SHA-256, and source-local unowned outgoing `relationships`; the same edges remain in the global inventory for graph-wide checks. Source-backed part bytes remain empty because the bounded `SourcePackageSnapshot` is the single byte owner. Content type and sorted per-part relationship signatures participate in graph comparison, so public-wire tampering fails before mutation. C# tests cover all three Office families, malformed manifests, exact custom-part/relationship preservation, and metadata tampering; the bundled-WASM smoke exercises the same public wire contract. |
| XLSX worksheet-table/connection wire boundary | partial | `WorksheetArtifact.tables` is additive field 9 and worksheet-level `sort_state` is additive field 10. Each independent `SpreadsheetTableArtifact` carries bounded semantics at fields 1–12, a field-13 `SpreadsheetTableSourceBinding`, field-14 rich columns, field-15 filters, field-16 sort state, and a field-17 source-bound `SpreadsheetTableQueryArtifact`. Query optional scalars preserve absent versus explicit false/zero; additive query field 24 independently carries recognized `queryTableRefresh` metadata and ordered fields. Refresh field 9 carries existing deleted-field names and field 10 reuses `SpreadsheetTableSortStateArtifact` for refresh-local sorting. Sort-state field 4 is optional `sort_method`, field 5 is optional presence-aware `column_sort`, and sort-condition field 5 is an optional value-only `custom_list`. Worksheet and QueryTable refresh sorts can use `column_sort=true` with unique single-row conditions spanning the sort columns; omission/false use unique single-column conditions spanning the sort rows. Table AutoFilter sorts reject either explicit `column_sort` value because SpreadsheetML restricts that attribute to non-AutoFilter ranges. `WorkbookArtifact.connections` is additive field 5; each `SpreadsheetConnectionArtifact` carries database/type-5 root metadata and optional refresh policy, while `SpreadsheetConnectionSourceBinding` proves ConnectionsPart path, whole-part/element/semantic hashes, and editability. Field IDs/count/order/table-column bindings, deleted-field count/order, sort presence/condition count, and connection count/order/ID/type/refreshed-version are immutable source topology; recognized names, bounded flags, sort selectors/direction, and non-secret connection policy are editable in place. Provider strings, commands, credentials, source paths, child graphs, unknown root attributes, extensions, and unsupported connection types never cross the semantic wire. `SpreadsheetTableColumnArtifact` independently models the name plus leading-equals calculated/totals formulas, array flags, built-in/custom totals function, and totals label while `column_names` remains compatibility-authoritative. `SpreadsheetTableFilterArtifact` keys one criteria branch by zero-based table column: field 2 maps exact values/blanks or grouped dates, field 3 custom comparisons, field 4 dynamic thresholds, field 5 Top10 criteria, field 6 icons, and field 7 stable colors. Sort conditions add icon field 3, color field 4, and custom-list field 5 while absence retains ordinary value sorting. Package relationship IDs remain locators, while XML/semantic hashes and editability bind safe second export. Source-free worksheet row/column sorts and ordinary tables plus recognized source-bound worksheet/table/QueryTable/connection profiles are editable. QueryTable/connection creation or execution, graph/field/deleted-field/sort/connection topology changes, multi-property differential formatting, and other complex native profiles remain exact-preserved or fail closed; opaque worksheet/query sort geometry and connection/history branches stay hidden while safe recognized edits continue. |
| XLSX worksheet metadata / workbook-view wire boundary | partial | `WorksheetArtifact.visibility` fields 11/12 retain bounded `<sheet>` semantics and hash-bound identity. `WorkbookArtifact.view` field 8 carries the primary active/selected worksheet IDs; additive `additional_views` field 9 carries only later windows. Every source binding records its zero-based workbook-view ordinal and one worksheet-view locator per sheet with part/view hashes, actual element ordinal, matching `workbookViewId`, and presence-aware source `tabSelected`. `XlsxWorkbookViewCodec` authors and imports complete multi-window graphs, maps each active ID to `ActiveTab`, maps each selected group to `SheetView.TabSelected`, and edits only those attributes while keeping window count/order and all unowned XML immutable. Missing/incomplete single-window sheet views stay readable but selection editing fails closed; incomplete multi-window graphs stay opaque. Active/selected visible tabs in any opaque window cannot be hidden. `workbook.windows`, stable window resolve IDs, primary compatibility APIs, inspect/layout/verify, JS fallback, bundled WASM, generated help, clean install, and the two-window runnable fixture share the contract. Window geometry/zoom/panes remain source-owned rather than semantically editable. |
| XLSX worksheet-drawing wire boundary | partial | `WorksheetArtifact.images` remains additive field 13 and `WorksheetArtifact.charts` is additive field 14. Pictures carry content-addressed assets, one-cell/two-cell/absolute anchors, crop/effects/transform, and a DrawingPart source binding. Each `SpreadsheetChartArtifact` carries model ID/name/title/type/legend, shared categories, ordered numeric series with optional category/value formulas, additive series fields 5–7 for RGB fill/direct line/marker, exactly one native-EMU anchor, field-11 `SpreadsheetChartSourceBinding`, primary-axis fields 12/13, field-14 title style, field-15 line options, and field-16 plot-level data labels. The line-options profile owns grouping/smooth/vary-colors presence; the independent label message owns direct value/category booleans and preserves present-all-false versus omission. The source binding records DrawingPart path/hash, child ordinal/hash, ChartPart path/hash, semantic hash, relationship locator, non-visual ID, and editability. Source-free authoring creates relationship-backed bar/line/pie ChartParts; source-bound edits retain anchor, chart type, series/point/reference topology, and axis identity while allowing recognized name/title/legend/formula/cache/fill/line/marker/grouping/smooth/vary-colors/data-label/font-size/axis values to change. `XlsxDrawingCodec` and the dedicated chart/style/marker/line-options/data-label/text/axis codecs share one pre-mutation drawing hash. Unsupported formula-referenced series names, complex style/marker/line-option/data-label/text graphs, rich titles, complex `txPr`, or logarithmic axes import read-only and exact-roundtrip. Imported add/remove, anchor/type/topology mutation, external data, combo/multi-plot charts, date/secondary/multi-axis profiles, axis-title styles, per-series/per-point/rich labels, groups, and shapes remain fail-closed or opaque. Relationship and axis IDs never become durable semantic identity. |
| XLSX workbook calculation wire boundary | partial | Additive `WorkbookArtifact.calculation` field 7 carries optional automatic/automatic-except-tables/manual mode, calculate-on-save, full-on-load, force-full, full-precision, iterative enable/count/delta semantics, and a hash-bound source locator. The JavaScript model exposes the same policy through `Workbook.create({ calculation })` and `workbook.setCalculation(...)`; both JS and OpenChestnut codecs roundtrip explicit false values. `XlsxCalculationCodec` authors a source-free `calcPr`, edits recognized imported fields in place, retains application `calcId`, and makes element presence immutable. R1C1 reference mode, calculation-completed state, concurrent controls, unknown attributes, and out-of-profile values stay opaque and exact-preserved; replacement or binding tamper fails closed. The codec records policy only and never claims to execute Excel recalculation. |
| C# Open XML SDK codecs for DOCX/PPTX/XLSX | partial | Source-built bounded readers/writers using public `DocumentFormat.OpenXml` 3.5.1 implement vertical slices for all three Office families with Office 2021 zero-error output validation. XLSX delegates picture/ImagePart semantics to `XlsxDrawingCodec`, image-byte validation/budgeting to `XlsxImageAssetCatalog`, GraphicFrame/ChartPart semantics to `XlsxChartCodec`, direct RGB series-fill ownership to `XlsxChartSeriesStyleCodec`, bounded direct series-line ownership to `XlsxChartSeriesLineStyleCodec`, direct line-marker symbol/size/RGB-fill/bounded-outline ownership to `XlsxChartSeriesMarkerCodec`, chart-level grouping/smooth/vary-colors ownership to `XlsxChartLineOptionsCodec`, plot-level value/category label ownership to `XlsxChartDataLabelsCodec`, chart-title/axis-tick font-size ownership to `XlsxChartTextStyleCodec`, and primary-axis ownership to `XlsxChartAxisCodec`; those modules share a DrawingPart without braiding drawing logic into worksheet cells. Advanced direct authoring and semantic editing remain on the JS codecs until schema/fixture parity expands. |
| Bundled .NET WebAssembly npm runtime | done | The npm tarball ships the source-built `dotnet.js` host, runtime/managed WebAssembly, boot and integrity manifests, licenses, CycloneDX SBOM, and public protobuf contract/binding. The authoritative GitHub source checkout retains the C# projects, locked dependencies, build scripts, tests, and reproducibility gates instead of duplicating them in the consumer tarball. Clean-install tests export/import XLSX, DOCX, and PPTX under Node with `dotnet` absent from `PATH`; Microsoft Office, LibreOffice, and network access are not runtime requirements. |
| Lazy JavaScript codec adapter and fallback | done | `open-office-artifact-tool/codecs/open-chestnut` is the canonical adapter: it lazily initializes one retry-safe cached runtime and exposes typed XLSX/DOCX/PPTX import/export/status plus an advanced public-wire invocation. `SpreadsheetFile`, `DocumentFile`, and `PresentationFile` share the exact public IDs `javascript` and `open-chestnut`, default to the JavaScript migration oracle, dynamically import OpenChestnut only when explicitly selected, strip the policy field before forwarding family options, and reject invalid IDs. The deprecated `codecs/openxml-wasm` subpath remains a tested, one-way re-export shim whose functions are identity-equal aliases and whose output reports `codec: "open-chestnut"`; the main facade deliberately rejects that old ID. |
| Cross-codec fidelity, safety, and package gates | partial | XLSX, DOCX, and PPTX retain bounded clean-install, source-preservation, Office 2021, and render-backed gates. The no-local-dotnet package probe crosses all three families through the main facade's explicit OpenChestnut policy, while facade tests cover both codec IDs, metadata identity, option forwarding, and fail-closed invalid/deprecated values; the three runnable Office skills use those same calls for real render-backed OpenChestnut roundtrips. XLSX coordinates worksheet visibility and multi-window selections with calculation/name/table/filter/sort/connection/query/theme/style/formula slices, XLDAPR dynamic arrays, embedded pictures, and bounded bar/line/pie charts with direct RGB fill/line, styled direct line markers, chart-level grouping/smoothing/vary-colors, plot-level value/category labels, font sizes, and primary axes without weakening opaque OPC checks. JavaScript fallback and bundled WASM assert the same bounded drawing projection; `open-chestnut-basic` crosses value→category labels alongside styled marker, grouping, smoothing, vary-colors, fill/line/font-size/axis edits through two codec passes. Current local/hosted gate evidence is refreshed in `docs/release.md` for each exact-SHA milestone. |

## Optional native Office automation target

| Requirement | Status | Notes |
| --- | --- | --- |
| Optional Node wrapper without core Windows/Office dependency | done | `open-office-artifact-tool/native/office-bridge` exposes `callOfficeBridge`, `renderFileWithNativeOffice`, `createNativeOfficeRenderer`, `nativeOfficeStatus`, temp-file isolation, cleanup, timeout handling, and structured `OfficeBridgeError` responses. Covered by mock-backed npm smoke tests. This is a renderer/application-compatibility adapter, not the core Office file codec. |
| C# JSON stdin/stdout sidecar | partial | `native/OfficeBridge` contains a .NET 8 sidecar that reads one JSON request from stdin and writes one structured JSON response to stdout. It supports `status`, `render`/`convert`/`export`, and Office-specific operation names. Local .NET 8 protocol tests pass on macOS arm64; Windows + Microsoft Office integration remains pending. |
| Windows Microsoft Office automation | partial | C# sidecar uses late-bound COM automation for Word DOCX PDF export/update fields/accept/reject revisions, Excel XLSX recalculation/autofit/PDF export, and PowerPoint PPTX PDF/PNG export. Office-specific integration tests remain gated by `OFFICE_NATIVE_TESTS=1` and require Windows + Office. |
| Dotnet tests for bridge protocol | done | `native/OfficeBridge/tests` covers status, structured errors, JSON serialization, CLI stdin/stdout status, and graceful no-Office behavior. `dotnet test native/OfficeBridge` passes locally with .NET 8 on macOS arm64 (`5` passed, `0` failed, `0` skipped). |

## Docs, examples, and CI

| Requirement | Status | Notes |
| --- | --- | --- |
| README clean-room/family/renderer/native bridge docs | partial | README covers clean-room goal, four artifact families, renderer adapters, optional Playwright renderer, PDF parser adapters, Node-side native Office wrapper, C# sidecar usage/tests, examples, release-check flow, and development commands. `docs/release.md` records current npm auth/publish blockers. Needs update once publishing/tagging succeeds. |
| Examples for DOCX/XLSX/PPTX/PDF/rendering | partial | `examples/` includes create DOCX report, create XLSX dashboard with charts/sparklines/comments, create PPTX compose deck with notes/comments/connectors, parse/render PDF, render via Playwright, and render via native Office bridge. Native bridge and Playwright examples skip gracefully when optional runtimes are unavailable; the Playwright example and renderer smoke now run locally with the installed Chromium runtime. |
| Project-local runnable agent skills | done | Clean-room project-local skills cover all four families. Spreadsheets produces XLSX/CSV/TSV semantic/native evidence; `open-chestnut-basic` crosses native worksheet tables, filters/sorts, theme/styles, shared/legacy/dynamic-array formula topology, all three picture anchors, bounded crop/effects/transform, same-format picture-byte replacement, and a line chart whose plot labels change from values to categories alongside grouping, smoothing, vary-colors, series fill/line/marker, title/tick-label font sizes, and primary axes between two WASM exports before model/package/Playwright/LibreOffice/Poppler QA. `open-chestnut-query-table` synthesizes a standards-based source graph and then calls OpenChestnut for import, safe refresh disablement plus root/refresh/field edits, two source-preserving exports, opaque-extension preservation, package inspection, and Playwright QA; native office rendering is intentionally off for this external-data fixture. Documents crosses its advanced business brief through OpenChestnut and native pages; Presentations adds per-slide modeled/native rendering, montage/baseline diffs, complex package fixtures, and an `open-chestnut-preservation` fixture for loss-aware master/layout/shape edits plus name/frame-only OLE/diagram/contentPart placement edits with exact recursive payload preservation; PDF adds real-byte/PDF.js extraction and Playwright/Poppler model/native diffs. All skills/scripts/fixtures ship in the npm package and have dedicated smoke tests. |
| Basic CI without Microsoft Office | partial | Linux CI and release jobs use Node 24-ready GitHub actions, explicitly install Playwright Chromium, LibreOffice Writer/Calc/Impress, Poppler, and .NET 8, verify each executable, then require all npm/skill/render/docs/pack, OfficeBridge, and OpenChestnut tests rather than relying on optional skips. Exact-SHA hosted run [`29445489984`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29445489984) passes the XLSX plot-level data-label increment on `main` at `0e9ef96b372279a610cdba895d4b8827145a5f9b`: public wire tags, JavaScript fallback and bundled-WASM parity, source-bound add/change/remove, false-residual preservation, complex-label fail-closed behavior, SVG/inspect, runnable skills, and real render-backed gates run alongside LibreOffice 24.2.7.2, Poppler 24.02.0, generated docs/proto and reproducibility checks, the 160-file no-local-dotnet clean install, OfficeBridge 5/5, OpenChestnut 143/143, and two byte-identical 38-file/13,648,062-byte runtime builds. Manual npm/tag/GitHub release remains gated by `NPM_TOKEN`. |
| Runnable presentation OLE-workbook QA | done | `open-chestnut-preservation` injects a valid source XLSX into an Office 2021-valid OLE object, imports through bundled OpenChestnut, replaces it with a second workbook through `replaceEmbeddedWorkbook(...)`, combines that edit with name/frame/master/layout/text changes, re-imports the final package, and asserts the replacement cell value. Negative fixtures also attach one workbook target to two slides and prove that both bundled OpenChestnut and the JavaScript fallback withhold the editable facade. The unchanged source preview deliberately continues to render while package/model/Playwright/LibreOffice/Poppler gates prove that payload replacement does not damage the surrounding deck. |

## Implementation priorities

1. Broaden the clean-room C# XLSX slice beyond its bounded worksheet-table/query-root/recognized-refresh-field/deleted-field/row-and-column-sort policy, source-bound database connection-root metadata, calculated-column/totals/exact/grouped-date/custom/dynamic/Top10/icon/color-filter/custom-list-value-icon-color-sort profile, primitive/cached/shared/legacy/dynamic-array formulas, views/dimensions/merges, themes, complete static cell styles, embedded pictures, and single-plot bar/line/pie charts with per-series solid RGB fills, bounded direct RGB line color/dash/width, styled direct line markers, title/tick-label font sizes, and primary category/value axes into additional non-secret connection profiles, multi-property differential styles, gradient/HSL/preset/scheme-color/theme-font/format-scheme/named-style semantics, complex multi-record cell-metadata profiles, advanced picture-effect graphs, secondary/logarithmic/date axes, theme/transformed/gradient/picture marker styles, compound/custom-dash lines, rich text, labels, combo/data sources, groups/shapes, and pivots while retaining source-bound preservation at every increment.
2. Expand third-party XLSX corpus, malformed-style/resource-graph, locale-dependent number-format, Excel/LibreOffice render, arbitrary-path, and JavaScript/OpenChestnut semantic gates before changing the default codec.
3. Broaden the shipped DOCX slice beyond hyperlinks, simple fields, source-free merge-table construction/fixed direct formatting, merge-aware imported table text, exact-profile imported formatting edits, direct text-marker numbering graphs, and coherent direct numbering-definition group edits into picture/style-linked graph authoring, nested/linked numbering edits, rich table-cell/theme/conditional/per-cell formatting, structural edits, and drawings/sections; broaden the shipped PPTX slice beyond direct paragraphs, shape-local list levels, bounded body properties, master text styles, direct Master/Layout backgrounds, and owner-local placeholder text into placeholder geometry/shape-style editing, background removal, transformed/other color semantics, and chart semantics. Preserve unsupported native objects on every migration step.
4. Continue format fidelity work where it strengthens the public model or both codec paths, including third-party Presentation chart data sources, DOCX numbering/pagination, and broader Excel formula/coercion coverage.
5. Broaden render-backed baselines across platforms and add non-translation registration beyond the shipped Playwright/sharp/canvas/Poppler/LibreOffice/native Office adapters.
6. Continue robust arbitrary-PDF extraction, font/image fidelity, accessibility, and cross-platform render baselines on top of the shipped PDF.js/Poppler workflow. PDF remains outside the Office OpenChestnut codec.
7. Expand `help` catalogs from the observed skill docs into generated, testable API records.
