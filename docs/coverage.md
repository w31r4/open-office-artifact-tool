# Clean-room coverage matrix

This document tracks the target behavior observed from four Office/PDF artifact workflows and the current implementation status of `open-office-artifact-tool`.

Status legend:

- `done`: implemented and covered by smoke tests.
- `partial`: API or minimal behavior exists, but fidelity/edge cases remain.
- `todo`: not implemented yet.

## Cross-cutting agent runtime

| Requirement | Status | Notes |
| --- | --- | --- |
| Shared `FileBlob` with `load`, `save`, `arrayBuffer`, `text` | done | Used by all four artifact families. |
| `inspect(...)` emits bounded NDJSON snapshots | partial | Implemented for workbook, presentation, document, and PDF—including normalized PDF table-cell records—with shared `search`/`searchTerm` filtering, `target`/`targetId`/`id`/`anchor` slicing against stable IDs/common anchors, `before`/`after`/`context` windows around target matches, and top-level field shaping via `include`/`fields` plus `exclude`/`omit`. Needs full token coverage and richer semantic context slices. |
| Stable anchor IDs + `resolve(...)` | partial | Implemented for workbook/sheets/tables/charts/images/sparklines/rules/comments, presentation theme/layout/slides/shapes/text ranges/tables/charts/images/connectors/comments, document theme/blocks/text ranges/bookmarks/headers/footers/comments/styles, and PDF pages/text/text-items/regions/tables/table-cells/images/charts. Needs richer imported-object identity preservation. |
| `help(query, opts)` bounded NDJSON API discovery | partial | Shared `HELP_CATALOG` and `helpArtifact(...)` power Workbook/Presentation/Document/PDF help methods. All 226 current catalog records have machine-readable parameter/return schemas: 134 Workbook/formula, 32 Presentation, 32 Document, 18 PDF, and 10 shared runtime records. `test/help.mjs` enforces 100% schema coverage, and `docs/api.md` is generated with field-level parameter/return details. Catalog breadth still needs to grow alongside future APIs and formula coverage. |
| Render/preview loop | partial | `renderArtifact(...)` plus per-artifact render/export adapters return SVG `FileBlob` previews with metadata for workbooks, presentations, documents, and PDFs. `renderArtifact(..., { format, renderer })` supports pluggable PNG/WebP/JPEG/PDF conversion adapters over the SVG/FileBlob source; optional adapters include Playwright SVG/HTML → PNG/WebP/JPEG/PDF, sharp SVG/PNG/JPEG/WebP → PNG/WebP/JPEG, node-canvas SVG/PNG/JPEG/WebP → PNG/JPEG, Poppler PDF → PNG/PPM/TIFF page rasterization, LibreOffice DOCX/XLSX/PPTX/HTML → PDF/office-supported conversions, and native Office via the Node/C# JSON bridge. Playwright + Chromium PNG/WebP/JPEG/PDF smoke tests pass locally on macOS arm64, including Chromium canvas-backed WebP encoding. Raster adapters hard-error when their optional peer dependency or CLI is missing rather than silently faking output. |
| Shared verification API | partial | `verifyArtifact(...)` plus per-artifact `verify()` methods emit bounded semantic/layout issues. `visualQaArtifact(...)` adds PNG/JPEG/WebP/PPM decoded-pixel metrics, cross-encoding comparison, decoded-pixel limits, suppression of byte-only false positives, and valid PNG `diffBlob` heatmaps. Heatmaps accept changed/unchanged RGB+alpha palettes and can align dimension-mismatched images strictly, top-left, or centered on a union canvas. Optional same-size pixel registration searches a bounded baseline translation with a capped sample budget, applies only above a configurable improvement ratio, ignores only translated edge pixels, and records sampled/exact before-after metrics plus the chosen offset in summary/blob metadata. All four runnable skills share one shipped baseline-set module for model/native PNG loading, writing, stale-file cleanup, diff persistence, and page/slide-count evidence. Supplying `baselineDir` is fail-closed: missing, empty, or non-contiguously numbered sets are rejected instead of silently skipping comparison. Registration CLI flags and heatmaps remain artifact-specific workflow outputs. Needs broader cross-platform baseline evidence and, eventually, non-translation registration. |
| Layout JSON export | partial | Slide layout JSON, document page-aware layout JSON, workbook/worksheet layout JSON, and PDF page layout JSON are implemented. All four layout families now support target/search/context slices with page/element/sheet pruning. Workbook layout cells include raw calculation `value` plus deterministic number/date-formatted `displayValue`, styles, merges, and pixel bounding boxes alongside tables, charts, images, sparklines, and rules; PDF layout JSON includes page text, positioned text, regions, tables with origin-cell spans/roles/header IDs/bounding boxes, images, and charts. Needs richer pagination/windowing and deeper semantic layout slices. |
| Durable file export/import smoke tests | done | Minimal XLSX/PPTX/DOCX/PDF round trips pass. |
| Safe reusable OOXML package layer | partial | XLSX/PPTX/DOCX inspect and patch APIs share bounded/path-safe package handling, content-type/relationship synchronization, namespace-aware source-reference validation, deletion cleanup, explicit-ID collision gates, and atomic final validation. Opt-in `sourceReference` builders add/remove DOCX settings semantics, header/footer references, batch classic-comment anchors, and paragraph numbering assignments; XLSX worksheet/table lists, worksheet drawings, DrawingML image/chart anchors, workbook pivot-cache lists, and cache-definition record IDs; plus PPTX slide/master/layout ID lists and image/chart objects in slide shape trees. Format decisions continue moving behind family modules. Dedicated deep modules own XLSX Office 2019 threaded comments, PPTX Office 2021 modern comments, DOCX cross-generation comment identity/people graphs, DOCX bookmark/internal-link planning, and DOCX section/header-footer declaration, activation, and inheritance. Arbitrary-path fixtures exercise add/remove/error/import/native-render paths. Needs richer pivot features and other structurally complex references. |

## Spreadsheets skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `Workbook.create`, worksheets collection, `getRange`, values/formulas | done | Matrix writes and formula recalc implemented. `Workbook.create({ dateSystem: "1904" })` / `workbook.setDateSystem(...)` select Excel's 1900 or 1904 serial-date context, expose it through inspect/help, and roundtrip native `workbookPr@date1904`. |
| `SpreadsheetFile.exportXlsx` / `importXlsx` | partial | XLSX roundtrip covers formulas, styles, validations, dynamic arrays, tables, pivots/caches, charts/images/sparklines/comments, worksheet views/dimensions, and clean-room metadata. In addition to the previously documented Theme/style, DrawingML, PivotTable grouping/filter/calculation/cache-policy, shared/array formula, and view/dimension behavior, Office 2019 threaded comments now use valid GUID identities and relationship-owned `WorkbookPersonPart`/`WorksheetThreadedCommentsPart` equivalents. Metadata-free import follows arbitrary people/thread targets, accepts alternate prefixes, preserves comment/person IDs, user/provider metadata, dates, parents and done state, distinguishes multiple roots on one cell, and preserves them on second export. Pivot calculated fields preserve 37 bounded scalar functions plus arithmetic/comparison/string/boolean expressions across metadata-free import and second export. Export explicitly declares every emitted worksheet Drawing part as `application/vnd.openxmlformats-officedocument.drawing+xml`, including drawings on sheets whose numeric index differs from the global image/chart part IDs; the runnable formula-summary now exercises nested logical/information/numeric/error/text/date functions under the 1904 date system and passes Open XML SDK Office 2021 validation with zero errors. Package inspection rejects wrong roots/content types/sources, external or multiple relationships, orphans, invalid/duplicate GUIDs, missing people/parents, cross-cell parents, and cycles. The fixture authors nested calculated Margin, Date Contract, and Date Shift Contract fields plus a resolved root/reply, imports them without metadata, and passes model/package/LibreOffice/Poppler gates. Needs grouped shapes, richer chart styling/calculated-field semantics, fuller dynamic-array extLst semantics, mentions/rich-text comment content, and broader Excel compatibility testing. |
| CSV/TSV import, export, inspect, and skill QA | done | `SpreadsheetFile.importDelimited/exportDelimited`, CSV/TSV convenience methods, and `inspectDelimited` implement bounded UTF-8 parsing/serialization with RFC-style quoted delimiters, escaped quotes, embedded newlines, BOM evidence, optional primitive coercion, calculated-value default export, explicit formula export, sheet/range selection, and byte/row/column limits. The runnable verifier accepts `.csv` and `.tsv`, emits bounded row evidence, and runs the same semantic/layout/visual gates; core and skill tests cover real CSV QA plus TSV roundtrip and malformed/oversized edge cases. Local macOS arm64 checks also pass both CSV and TSV through Playwright plus LibreOffice→PDF→Poppler native page verification. Delimited files intentionally flatten one worksheet and cannot preserve XLSX styling/drawings. |
| Formula calculation | partial | Supports arithmetic refs, defined names, Excel table sections/ranges/unions/space intersections, apostrophe-escaped special-character headers, unqualified calculated-column references, explicit `@`/`#This Row` row context, native shared/array formula import/export, and workbook dependency graphs with dependents/cycles/missing-sheet errors. Structured intersections return only cells common to every operand, support section and chained column overlap, and emit `#NULL!` when disjoint. Structured evaluation, graph, and trace share one dedicated syntax parser and expose only effective intersection precedents; metadata-free native XLSX roundtrips preserve the formula. The runnable `structured-intersection` fixture covers package/inspect/Playwright QA. The locally installed LibreOfficeDev 26.8 alpha displays `Err:509` for direct native execution but rewrites the expression to an A1-range intersection during ODS roundtrip; the engine accepts that rewrite and restores its exact value/precedents. Microsoft Excel execution remains pending Windows validation. Pivot calculated fields use a separate bounded AST over summed source fields. Its 37-function scalar whitelist covers 12 numeric helpers, `AND`/`OR`/`NOT`, lazy `IF`/`IFERROR`/`IFNA`, `NA`, `ISERROR`/`ISNUMBER`/`ISTEXT`, Excel Compatibility Version 2 surrogate-aware `LEN`/`LEFT`/`RIGHT`/`MID`, case conversion, ASCII-space `TRIM`, and workbook-date-system-aware `DATE`/`YEAR`/`MONTH`/`DAY`/`EDATE`/`EOMONTH`/`DAYS`/`WEEKDAY`; month shifts truncate their offset and clamp short months, while `WEEKDAY` supports return types 1, 2, 3, and 11 through 17. The evaluator retains the 1900 serial-60 leap-day quirk and 1,462-day 1900/1904 offset. It distinguishes literal `#`-prefixed text from actual Excel errors, validates unused branches, and forbids cell references or dynamic execution. Current-row references outside the table return `#VALUE!`. `COUNTIF(S)`, `SUMIF(S)`, and `AVERAGEIF(S)` share case-insensitive numeric/text comparison plus Excel `?`/`*`/`~` wildcard semantics; multi-range variants enforce equal dimensions and matched sum/average errors propagate. Dynamic arrays include selection, reshape, stack, and expansion helpers through `HSTACK`, `VSTACK`, and `EXPAND`. Lookup coverage includes `XMATCH` exact, next-smaller, next-larger, wildcard, forward/reverse, and sorted-search modes. Statistical/ranking coverage includes `MEDIAN`, `MODE.SNGL`, `LARGE`, `SMALL`, and `RANK.EQ`; signed decimal rounding includes `ROUND`, `ROUNDUP`, and `ROUNDDOWN`. Deterministic Excel 1900/1904 date arithmetic now includes `DATEVALUE`, `TIME`, `TIMEVALUE`, `HOUR`, `MINUTE`, and `SECOND` alongside `DATE`, `YEAR`, `MONTH`, `DAY`, `EDATE`, `EOMONTH`, `DAYS`, `WEEKDAY`, `NETWORKDAYS`, `WORKDAY`, `NETWORKDAYS.INTL`, and `WORKDAY.INTL`. The dedicated `src/spreadsheet/formula-coercion.mjs` accepts strict ISO and English month-name dates with four-digit years, 12/24-hour times, and `VALUE` text using ASCII grouping/scientific/percent/accounting syntax; ambiguous host-locale dates fail with `#VALUE!`. It retains the 1900 serial-60 quirk, 1,462-day system offset, 1904 serial zero, TIME overflow bounds/wrapping, invalid date/time-prefix validation, and date-system-specific range checks. Core tests cover invalid/ambiguous/overflow paths plus 1900/1904 XLSX roundtrip. The runnable formula-summary adds a third DateTimeText sheet and passes model, all-sheet Playwright, LibreOffice/Poppler, and native ODS→XLSX calculation gates. Error handling distinguishes `IFERROR`/`ISERROR` from #N/A-specific `IFNA`/`ISNA`, supports `ISERR`, and can emit `NA()`. The 88-entry formula help catalog spans math/statistical/logical/information/text/lookup/date-time/dynamic-array helpers. Needs broader configurable locale coercion, more array/coercion semantics, and closer Excel error parity. |
| `workbook.inspect` table/formula/match/sheet/workbook | partial | Core records implemented for workbook, sheet (including gridline/frozen-pane view state, custom dimension counts, and merged-range counts), mergedCell, row/column dimensions, table/structured table, definedName, formula, formula graph, match, style/computedStyle, drawing/chart/image, sparkline, thread, dataValidation, and conditionalFormat with search, target, context-window, and include/exclude filtering. Needs richer semantic context slicing and broader imported-object identity preservation. |
| `workbook.render` visual verification | partial | SVG grid preview and layout JSON consume native-equivalent per-column widths, per-row heights, hidden axes, merged-cell spans, number/date-formatted display values, symbolic theme/tint/indexed colors, patterned fills, extended font/alignment styles, table/chart/image/sparkline hints, conditional-format fills, sliced rules, gridline visibility, and frozen-pane state. The live `range.format` facade supports character/point and pixel sizing plus bounded deterministic `autofitColumns()`/`autofitRows()`. `range.fillDown()`/`fillRight()` copy contents and formatting while translating relative A1 formula references; merged intersections fail explicitly. With `--all-sheets true`, the runnable skill emits every worksheet's layout/Playwright preview and per-sheet baseline. `--native-render required` additionally gates the real XLSX through LibreOffice PDF and Poppler page PNGs, compares every native page, and rejects pagination changes; this path is exercised locally when tools are available. Needs full drawing fidelity, broader locale-specific number-format rendering, broader cross-platform evidence, and Windows Excel baselines. |
| `workbook.trace` | partial | Formula tracing now uses the same dependency model as recalculation, exposes precedent trees, graph nodes/edges via `workbook.formulaGraph()` and inspect kinds (`formulaNode`, `formulaEdge`, `formulaCycle`), resolves escaped/current-row/range/union/intersection table references through the shared parser, reduces intersections to their exact common-cell precedents, and reports cycles/missing-sheet refs to verify. Needs broader parser coverage and large-trace summarization. |
| Charts/tables/sparklines/images/comments/data validations/conditional formats | partial | Workbook Office 2019 threaded comments, validations/conditional formats, pivots, native tables, charts, images, sparklines, inspect/resolve/render hints, and consistency verification are implemented. Thread inspect records expose preserved native comment/person IDs and dates; verification rejects invalid modeled native metadata before export. Relationship-driven DrawingML and PivotTable behavior retains the documented metadata-free roundtrip and runnable/native QA; calculated-field coverage includes arithmetic, comparisons, concatenation, strings/booleans, 12 numeric functions, logical/information predicates, lazy general/#N/A error fallbacks, seven bounded text functions, and eight date functions with explicit 1900/1904 context. Needs threaded-comment mentions/rich text, richer chart styling, grouped shapes, business-day and time-of-day calculated-field functions, richer conditional formats, and broader Excel compatibility testing. |

## Presentations skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `Presentation.create`, slides collection, `slide.shapes.add` | done | Basic slide/shape/text facade works. |
| `PresentationFile.exportPptx` / `importPptx` | partial | PPTX roundtrip covers direct and grouped DrawingML shapes, connectors, tables, charts, pictures, notes, comments, themes, layouts, rich text paragraphs with relationship-backed hyperlinks, and read-only opaque native objects. `shape.text.set(...)` accepts ordered paragraphs and styled runs, Unicode or relationship-backed picture bullets, standard DrawingML auto-number schemes, marker font/color/fixed-or-percentage size, explicit follow-text marker overrides, levels, alignment, indents, point/percent spacing, and run formatting; inspect/layout/SVG and native export share the normalized model. Structured runs accept one non-fetched absolute external URI or internal slide ID plus optional tooltip, target frame, history, and click-highlight state. The dedicated `src/presentation/ooxml-picture-bullets.mjs` planner now owns direct/grouped, Slide Master `p:txStyles`, master-placeholder, and Slide Layout placeholder picture bullets. Embedded PNG/JPEG/GIF/SVG data URLs share media bytes across owners while each owning OOXML part receives its own collision-free image relationship; external URIs remain non-fetched with `TargetMode="External"`. Metadata-free import follows the owner part's `.rels`, rejects missing/wrong-type/missing-part targets, accepts alternate relationship prefixes and strict OOXML image relationship URIs, restores editable inherited styles, honors direct marker overrides, and preserves them through second export. Core and runnable-skill fixtures cover master/layout/slide ownership and inherited placeholders; standalone authored and second-export packages both pass Open XML SDK Office 2021 with zero errors. Local LibreOfficeDev 26.8 opens and renders the package but remains inconsistent: master-placeholder and layout-placeholder image markers are visible while the Slide Master `p:txStyles` marker is omitted; Windows PowerPoint visual validation remains pending. Placeholder geometry and other paragraph properties cascade through Slide Master `p:txStyles` title/body/other levels, master placeholders, layouts, and slides. The dedicated `src/presentation/ooxml-charts.mjs` codec owns bounded standard style IDs (`styleId`/`styleIndex`), color variation, horizontal/vertical bar direction, clustered/stacked/percentage grouping, gap/overlap, series fill plus bounded line width/dash styling, indexed `c:dPt` fill/line overrides, chart/per-series line markers and smoothing, primary/secondary-axis bar, line, and bar+line combo plot groups, chart/per-series value/category label visibility and positions, and series trendlines. Label overrides accept long public aliases or native OOXML positions, can explicitly disable inherited chart labels, feed inspect/layout/SVG, accept alternate prefixes, and survive metadata-free second export. `trendline`/`trendlines` accept all six standard DrawingML types with public aliases, type-specific bounded polynomial order or moving-average period, bounded half-category forward/backward extension, optional intercept/name/equation/R-squared flags, and line styling; all types feed inspect/layout/native OOXML and metadata-free second export. The dedicated `src/presentation/chart-trendline-svg.mjs` now previews linear, exponential, logarithmic, moving-average, polynomial, and power fits with bounded deterministic sampling on vertical/horizontal bars, lines, combo plots, and primary/secondary scales; it omits invalid positive-domain fits instead of emitting non-finite SVG. Bar/line series may split one plot type across primary and secondary OOXML plot blocks; combo series require an explicit bar/line type, retain global series order across all plot groups, preserve both option sets plus axis ownership, expose mixed SVG/layout/inspect output, accept alternate prefixes, and survive metadata-free second export. Independent authored and metadata-free second-export combo, label-override, trendline, secondary-axis, and same-type split-axis decks pass Open XML SDK Office 2021 with zero errors; the runnable `agent-readiness` fixture passes model/package gates and visibly renders columns plus primary- and secondary-axis line groups, markers, category/value labels, styled linear and polynomial trendlines, error bars, and legend through LibreOffice/Poppler. Authored and python-pptx point/series-format decks pass Open XML SDK Office 2021 with zero errors and visibly retain dashed lines, overridden fills, and point borders through LibreOffice/Poppler. Metadata-free text import likewise accepts alternate prefixes and preserves third-party master-only character bullets, marker fonts, nested margins, font sizes, and percentage spacing through second export. Fixed Georgia/red/150% and follow-text auto-number fixtures plus a python-pptx `•`/`–`/`•` title/content deck pass Open XML SDK Office 2021 validation with zero errors and render correctly through LibreOffice/Poppler. The dedicated `src/presentation/ooxml-hyperlinks.mjs` planner owns collision-free external-hyperlink and internal-slide relationships for direct/grouped shapes plus Slide Master and Slide Layout placeholders. It emits `a:hlinkClick`, de-duplicates repeated targets per owner, resolves metadata-free internal targets back to current model slide IDs, accepts alternate namespace prefixes and strict relationship URIs, rejects missing/wrong-mode/wrong-type/missing-slide relationships, and preserves links through second export. Core tests cover all owners; isolated authored and metadata-free second-export decks pass Open XML SDK Office 2021 validation with zero errors, while the runnable `agent-readiness` fixture exercises external URI model/package/SVG and LibreOffice/Poppler QA. Unsupported top-level or grouped `p:contentPart` and `p:graphicFrame` children become agent-visible read-only `nativeObject` facades with recursively preserved OPC graphs; real python-pptx OLE and Open XML SDK SmartArt assets also pass validation after second export. Native-object semantic editing, action-only/custom-show hyperlinks, content-part-specific preview rendering, and broader third-party PPTX fidelity remain gaps; unsupported modern-comment targets still use `unknownAnchor`. |
| `presentation.inspect` slide/textbox/shape/layout | partial | Stable records implemented for deck, theme, all Slide Masters, layout templates, slides, textboxes, text ranges, shapes, grouped-shape trees, tables, charts, images, connectors, read-only native content-part/OLE/diagram objects, notes, comments/threads, and layout, with shared search/target/context/include/exclude filtering. Native records expose classification, source part, relationship/part counts, geometry when present, and `editable:false`. Needs richer target-specific semantic slices. |
| Presentation chart error bars | partial | Series `errorBars` supports x/y direction, both/minus/plus extent, fixed value, percentage, standard deviation, standard error, and per-point custom plus/minus arrays with strict series-length validation, end-cap control, and bounded line styling. Custom sources accept bounded `plusFormula`/`minusFormula` references, aliases, optional caches and format codes, and require relationship-backed embedded XLSX bytes or an absolute non-fetched external workbook URI. Embedded bytes pass the XLSX package inspector before export. The dedicated chart/data codecs emit schema-ordered `c:errBars` with `c:numLit` or `c:numRef`/`c:f`/`c:numCache`, `c:externalData`, chart relationships, content types, and embedded workbook parts; import accepts alternate namespace prefixes, preserves formula-only references plus embedded bytes or external URIs, and survives metadata-free second export. Package validation rejects missing/wrong-type/orphaned relationships and invalid workbook content types. SVG previews scalar and cached custom error bars on line, column, and horizontal-bar series. Authored and second-export formula-reference decks pass Open XML SDK Office 2021 with zero errors and visibly render through LibreOffice/Poppler; the runnable `agent-readiness` fixture builds its embedded workbook through the public spreadsheet API and passes model/package/native gates. Broader Windows PowerPoint evidence remains pending. |
| Presentation chart secondary axes | partial | Bar, line, and combo-chart series accept `axisGroup: "secondary"` plus bounded `y2`/boolean aliases while retaining at least one primary series. Export partitions each bar/line type into primary and secondary OOXML plot blocks, emits one category/value pair per used axis group with unique IDs, top/right positions, symmetric `crossAx` references, and `crosses="max"`, and preserves global `c:order` values across up to four combo groups. Import traverses every repeated `c:barChart`/`c:lineChart` block, identifies ownership from its axis IDs and positions, accepts alternate namespace prefixes, restores secondary titles, and preserves global series order through metadata-free second export. Inspect/layout expose axis ownership; SVG computes stacked totals and maxima per axis group and draws a visible right-side axis. Core tests cover bar/line/combo splits plus invalid groups, pie rejection, missing primary series, and configured-but-unused axes. Authored and second-export same-type line and bar decks pass Open XML SDK Office 2021 with zero errors and visibly render independent left/right scales through LibreOffice/Poppler; the runnable fixture exercises primary-column plus primary/secondary-line groups through Playwright and native 3/3-slide QA. Windows PowerPoint evidence remains pending. |
| `presentation.resolve` | partial | Resolves presentations, themes, all Slide Masters, slide layouts, slides, shapes, nested grouped shapes/children/text ranges, tables, charts, images, connectors, native content-part/OLE/diagram facades, and comment threads. Native payloads intentionally remain read-only until format-specific public editing contracts exist. |
| `slide.export({format:'layout'})` | done | Layout JSON covers shapes, grouped-shape trees with local and absolute frames, textboxes, tables, charts, images, connectors, and read-only native-object placeholders, with target/search/context slicing over stable element, textRange, native-object, and comment IDs. |
| `presentation.export` image preview/montage | partial | SVG preview covers modeled elements and deck montage thumbnails. The Presentations skill renders every modeled slide with Playwright, creates a montage, gates the real PPTX through LibreOffice PDF + Poppler per-slide PNGs, compares model/native baselines, cleans stale slide baselines when approving updates, and rejects slide-count changes. Needs richer DrawingML fidelity and broader cross-platform baseline evidence. |
| Compose/JSX layout + token parser | partial | Helper-node compose engine implemented for row, column, grid, layers, box, paragraph, run, shape, table, chart, image, and rule with fill/hug/fixed sizing, fr/fixed grid tracks, spans, gaps, padding, stable names/ids, text class tokens, inspect, resolve, layout JSON, and PPTX roundtrip. `slide.autoLayout` now places existing shapes with horizontal/vertical flow, frame, gap, padding, and alignment. Package exports now include `./presentation-jsx`, `./presentation-jsx/jsx-runtime`, and `./presentation-jsx/jsx-dev-runtime` with `jsx`, `jsxs`, `jsxDEV`, `Fragment`, helper nodes, and function component support. Table nodes emit native PPTX DrawingML tables; chart nodes emit native PPTX bar/line/pie and primary/secondary-axis bar/line and bar+line combo chart parts with repeated same-type plot groups with title/category/series/color/axis/legend/data-label/trendline/error-bar metadata plus standard style, bar grouping/direction/gap/overlap, series/point fill and line styling, and line marker/smoothing options; and `dataUrl` images emit native PPTX image parts. Scalar, literal-custom, and formula-reference error bars preserve native OOXML/data parts and render from cached values in SVG. Needs fuller token parser and richer label styling. |
| Overlap/overflow/template fidelity QA | partial | `slide.validateLayout()` and `presentation.validateLayout()` detect off-canvas elements, connector endpoints outside the slide, geometry overlaps, basic text overflow, and table cell overflow; `presentation.verify()` also detects dangling comment targets, required placeholder/template gaps, chart/category/series mismatches, non-numeric chart data, table shape issues, and empty/invalid image data. The runnable skill adds full-size per-slide model/native review and optional baseline diffs. Needs richer placeholder/template fidelity gates and Windows PowerPoint evidence. |

## Documents skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `DocumentModel.create`, paragraphs, lists, headers/footers, hyperlinks, bookmarks, fields, citations, images, sections, tables, comments, styles, `inspect` | partial | Block model now supports an agent-facing Word theme, document-wide default run properties, paragraph and character styles with `basedOn` chains, `runStyleId` character-style references, styled paragraph spans with source-plus-resolved values, multi-level list items, headers/footers, external and internal hyperlinks, single- and multi-block bookmark ranges whose endpoints may be blocks or exact `table.getCell(row, column)` facades, native-tagged citations plus independent bibliography sources, fields, images, sections, tracked changes, tables, comments, textRange/tableCell anchors, inspect/resolve/search, page-aware layout, SVG preview, and help entries. `document.addBibliographySource(...)` models Word source types, personal/corporate authors, publication fields, source tags and style metadata; citations automatically create or reuse their tagged source. Effective run formatting uses Word precedence—document defaults, paragraph chain, character chain, direct formatting—and feeds layout/SVG; explicit false values and direct/theme conflicts override correctly. Settings cover revision tracking, field refresh, even/odd headers, mirrored margins, and passwordless editing restrictions. Per-section settings preserve different-first-page activation separately from first-page references; layout pages expose selected default/first/even IDs, source section, inherited/blank state, and page-in-section. Needs richer pagination and text measurement, especially decoration changes inside a continuous section page. |
| `DocumentFile.exportDocx` / `importDocx` | partial | WordprocessingML round trip writes relationship-backed Theme, settings, styles, classic comments, Office 2013 `commentsExtended`, Office 2019 `commentsIds`, Office 2021 `commentsExtensible`, Word `people`, numbering, header/footer, external/internal links, bookmark ranges, fields, native `CITATION` fields plus `b:Sources` custom XML, images, sections, tracked changes, lists, and tables. The dedicated `src/ooxml/docx-bibliography.mjs` codec validates all 17 public Word source types and bounded source/style fields, serializes personal/corporate authors with deterministic GUIDs, follows arbitrary `customXml` relationship targets, accepts alternate namespace prefixes, restores cited and uncited sources plus field results through metadata-free import/second export, and rejects duplicate/invalid/unlinked sources or dangling citation tags at the package boundary. The dedicated comments, links, and sections codecs retain their documented durable identity, exact block/table-cell range, and section inheritance behavior. The runnable business-brief fixture exercises an ECMA-376 native citation and an uncited Open XML SDK source together with its multi-block/table-cell bookmarks, section inheritance, durable comments, package/model/Open XML SDK, and native-render QA. Needs picture bullets and full numbering-style inheritance. |
| Design presets / table geometry / OOXML patch helpers | partial | `document.applyDesignPreset()` and inherited effective styles feed inspect/layout/render/export. `DocumentFile.inspectDocx()` and `patchDocx()` use the shared bounded, path-validated, atomically result-validated OOXML package layer. Settings, header/footer, classic comments, `commentsExtended`, `commentsIds`, `commentsExtensible`, `people`, and numbering recipes retain arbitrary-target relationship and semantic-validation behavior. Runnable `business-brief`, `package-settings`, `package-comments`, and `package-numbering` fixtures restore native style cascades, theme/run semantics, settings, comment identities/targets, people metadata, and multilevel list metadata with `{ preferNative:true }`, then pass LibreOffice→PDF→Poppler. The generated business brief also passes Open XML SDK Office 2021 validation with zero errors after schema-ordering table properties. `document.verify()` covers unknown paragraph/character styles, invalid links/citations, section indexes, malformed images/sections/tables, comment identity/person errors, and modeled overflow. Needs additional settings such as write protection/proof state only when agent workflows demonstrate demand. |
| DOCX render-to-page images | partial | `document.render()` provides a clean-room SVG/page-layout preview and `renderArtifact(..., { source: 'docx', renderer })` can feed the real DOCX into LibreOffice/native Office adapters. The runnable Documents skill now creates and compares a Playwright model PNG baseline plus every LibreOffice→PDF→Poppler native page PNG, records per-page pixel metrics, rejects changed page counts, and exercises this flow locally when the installed native tools are available. Needs richer pagination fidelity, broader cross-platform baseline evidence, and Windows Word comparison. |

## PDF skill target

| Requirement | Status | Notes |
| --- | --- | --- |
| `PdfArtifact.create`, `PdfFile.exportPdf`, `PdfFile.importPdf` | partial | Multi-page modeled PDFs support positioned text, tables, images/charts, stable IDs, and real PDF 1.7 export with PNG/JPEG XObjects. The dedicated `src/pdf/table-grid.mjs` normalizes table origin/covered cells, stable cell IDs, zero-based `cells` overrides, editable `table.getCell(row, column)` facades, row/column spans, `TH`/`TD` roles, `Row`/`Column`/`Both` scopes, explicit/inferred header associations, cell bounding boxes, and overlap/range/header validation. SVG, layout JSON, inspect/resolve/extract, vector PDF drawing, and the tagged structure tree consume that same grid. Export is tagged by default with Catalog language, MarkInfo, StructTreeRoot/ParentTree, per-page StructParents, cell-level MCIDs and IDs, H1/P/Figure elements, semantic `Table` → `TR` → `TH`/`TD` structure, and standard Table-owner `RowSpan`/`ColSpan`/`Scope`/`Headers` attributes. `inspectPdf()` reports cell-ID, span, header-association, tagged/language/marked-content, font, and per-role evidence. The runnable fixture includes a two-level column header with both row and column spans plus an explicit association; its structural gate derives expected counts from normalized origin cells. Optional standalone glyf-based TrueType input produces embedded Type0/CIDFontType2 objects, Identity-H CID encoding, explicit CIDToGID mapping, widths and a `ToUnicode` CMap. Default deterministic subsetting preserves used glyphs plus recursive composite dependencies and rebuilds `glyf`/long-`loca`/format-12 `cmap`, sfnt checksums and `checkSumAdjustment`; `{ subsetFont:false }` retains diagnostic full-font output. Unsafe fallback replacement is forbidden, and malformed/oversized fonts, `.ttc` collections and missing glyphs hard-error. Public Liberation Sans tests reduce a Unicode fixture from about 93 KB full-font to 16 KB subset; local Chinese validation reduces about 15.2 MB to 70 KB while PDF.js extraction and Poppler pixels remain correct. Import supports full modeled-span metadata roundtrip, injected parsers, geometry-based simple-table reconstruction, and PDF.js. Needs complex-script shaping, metadata-free spanning-cell reconstruction from arbitrary PDFs, richer reading-order controls, arbitrary-PDF corpus evidence, formal PDF/UA validation, and broader device-independent color-space/profile handling. |
| PDF visual render verification | partial | SVG page render draws modeled text, positioned text items, tables, image regions, and charts; layout JSON is page-sliced. The PDF skill renders every modeled page with Playwright and every real PDF page with Poppler, verifies page counts with `pdfinfo`, compares model/native PNG baselines, cleans stale baselines on approval, and rejects baseline page-count changes. The two-page fixture passes this full loop on macOS arm64. Needs broader cross-platform and arbitrary-PDF baseline evidence. |
| Text/table extraction | partial | `extractText()` and `extractTables()` work for modeled/generated PDFs; extracted modeled tables include normalized origin cells, spans, roles, scopes, explicit/effective header IDs, and cell bounding boxes. Import detects pipe-delimited/aligned tables and reconstructs simple geometry tables. PDF.js parses real bytes into text items, simple tables, actual bounded PNG image data when raw XObject pixels are available, and fill-colored RGBA PNGs for 1-bit stencil/solid masks; the runnable skill records real-byte parsing alongside Poppler evidence. Needs richer multi-line and metadata-free spanning-cell reconstruction plus complex device-independent color-space conversion. |
| Create polished reports with typography/layout/charts | partial | Modeled creation/export supports positioned text, span-aware vector tables with multi-level accessible headers, PNG/JPEG images, contain/cover placement, clipping, a tagged-PDF accessibility baseline with `Table`/`TR`/`TH`/`TD` hierarchy and standard table attributes, and caller-supplied subsetted Unicode TrueType embedding. `pdf.addFlowText(...)` estimates public Helvetica metrics, wraps paragraphs and long tokens inside margins, continues below existing text, appends same-size pages automatically, and preserves stable flow metadata. The runnable fixture forces a third page and requires tagged/language/MCID/structure/span/header-association evidence alongside PDF.js, Playwright, Poppler, page-set baselines, and package objects; `--font` enables Unicode fixture creation and `--subset-font false` enables diagnostic full-font comparison. Needs complex-script shaping, richer typography/chart styling, reading-order controls, color profiles, and formal PDF/UA validation. |

## Native Office bridge target

| Requirement | Status | Notes |
| --- | --- | --- |
| Optional Node wrapper without core Windows/Office dependency | done | `open-office-artifact-tool/native/office-bridge` exposes `callOfficeBridge`, `renderFileWithNativeOffice`, `createNativeOfficeRenderer`, `nativeOfficeStatus`, temp-file isolation, cleanup, timeout handling, and structured `OfficeBridgeError` responses. Covered by mock-backed npm smoke tests. |
| C# JSON stdin/stdout sidecar | partial | `native/OfficeBridge` contains a .NET 8 sidecar that reads one JSON request from stdin and writes one structured JSON response to stdout. It supports `status`, `render`/`convert`/`export`, and Office-specific operation names. Local .NET 8 protocol tests pass on macOS arm64; Windows + Microsoft Office integration remains pending. |
| Windows Microsoft Office automation | partial | C# sidecar uses late-bound COM automation for Word DOCX PDF export/update fields/accept/reject revisions, Excel XLSX recalculation/autofit/PDF export, and PowerPoint PPTX PDF/PNG export. Office-specific integration tests remain gated by `OFFICE_NATIVE_TESTS=1` and require Windows + Office. |
| Dotnet tests for bridge protocol | done | `native/OfficeBridge/tests` covers status, structured errors, JSON serialization, CLI stdin/stdout status, and graceful no-Office behavior. `dotnet test native/OfficeBridge` passes locally with .NET 8 on macOS arm64 (`5` passed, `0` failed, `0` skipped). |

## Docs, examples, and CI

| Requirement | Status | Notes |
| --- | --- | --- |
| README clean-room/family/renderer/native bridge docs | partial | README covers clean-room goal, four artifact families, renderer adapters, optional Playwright renderer, PDF parser adapters, Node-side native Office wrapper, C# sidecar usage/tests, examples, release-check flow, and development commands. `docs/release.md` records current npm auth/publish blockers. Needs update once publishing/tagging succeeds. |
| Examples for DOCX/XLSX/PPTX/PDF/rendering | partial | `examples/` includes create DOCX report, create XLSX dashboard with charts/sparklines/comments, create PPTX compose deck with notes/comments/connectors, parse/render PDF, render via Playwright, and render via native Office bridge. Native bridge and Playwright examples skip gracefully when optional runtimes are unavailable; the Playwright example and renderer smoke now run locally with the installed Chromium runtime. |
| Project-local runnable agent skills | done | Clean-room project-local skills now cover all four families. Spreadsheets produces XLSX/CSV/TSV import, inspect, formula/verify/layout/visual evidence, native theme/pattern-style roundtrips, and native page gates; Documents adds package inspection, Office 2013/2019/2021 threaded/durable/people comment evidence, relationship-free internal hyperlinks, block/table-cell bookmarks, native bibliography sources/CITATION fields, and LibreOffice/Poppler page gates; Presentations adds per-slide model/native rendering, montage review, baseline diffs, two-master ownership plus distinct-theme evidence, an arbitrary-path package DrawingML fixture, and a notes/comments relocation fixture that proves zero semantic issues plus restored author identity; PDF adds real-byte inspection, PDF.js extraction, per-page Playwright/Poppler rendering, and model/native PNG baseline diffs. All skills/scripts/fixtures ship in the npm package and have dedicated smoke tests. |
| Basic CI without Microsoft Office | partial | Linux CI and release jobs use Node 24-ready GitHub actions, explicitly install Playwright Chromium, LibreOffice Writer/Calc/Impress, Poppler, and .NET 8, verify each executable, then require all npm/skill/render/docs/pack and OfficeBridge tests rather than relying on optional skips. Hosted runs `29161517952` and `29161767731` passed this complete gate on `main`; Windows-only COM code is platform-annotated so cross-platform builds stay warning-clean. Package-content and license-policy gates require all skills/native/notices and reject reference/build outputs. Manual npm/tag/GitHub release remains gated by `NPM_TOKEN`. |

## Implementation priorities

1. Continue moving format-specific package and source-reference decisions behind family modules; next targets are richer Pivot calculated-field semantics and broader third-party Presentation chart data-source fidelity on top of the shipped owner-aware picture-bullet, primary/secondary-axis same-type/combo-chart, custom error-bar formula/data-part, paragraph/list inheritance, and opaque native-object codecs.
2. Continue broadening Excel formula/coercion coverage on top of the shipped dependency graph.
3. Improve presentation modern-comment targeting for native objects, broader third-party chart data sources, and broader third-party master/theme fidelity on top of the shipped owner-aware picture bullets, primary/secondary-axis same-type/combo charts, six-type model/native trendlines, per-series data labels/formula-backed error bars, series/point styling, paragraph/list/marker-typography cascade, per-master Theme, and opaque content-part/OLE/diagram graph.
4. Broaden render-backed baselines across platforms and add non-translation registration beyond the shipped Playwright/sharp/canvas/Poppler/LibreOffice/native Office adapters.
5. Improve DOCX pagination (especially continuous-section page decoration), picture bullets/full numbering-style inheritance, and broader Word compatibility on top of the shipped comments identities, native bibliography/CITATION parts, block/table-cell bookmark/internal-link codec, section/header-footer planner, native Theme/run-style codecs, real render gate, and design presets.
6. Continue robust arbitrary-PDF extraction, font/image fidelity, accessibility, and cross-platform render baselines on top of the shipped PDF.js/Poppler workflow.
7. Expand `help` catalogs from the observed skill docs into generated, testable API records.
