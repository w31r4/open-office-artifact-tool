# Coverage

This document describes the supported 0.2 boundary. It is not a promise that every legal OOXML or PDF construct is editable.

Status meanings:

- **done**: the modeled 0.2 operation is supported for creation/import, edit, export, and second import where applicable.
- **partial**: a narrower profile is editable, or imported content is preserved but some creation/editing is intentionally rejected.
- **unsupported**: no modeled operation is provided; Office export must fail rather than flatten the object if preservation cannot be proven.

## Architecture

| Surface | Status | Boundary |
| --- | --- | --- |
| XLSX/DOCX/PPTX facade path | done | All six Office facade methods lazily load the canonical OpenChestnut C# WASM adapter. There is no codec selector or JS Office fallback. |
| PDF path | done | Independent PDF implementation; never enters the OpenChestnut OOXML wire. |
| PDF provider routing | partial | Required `mupdf@1.28.0` is resolved by normal npm installation and lazily initialized on the first PDF operation. `PdfFile` plus the thin `scripts/mupdf.mjs` CLI provide the default arbitrary-file parse/inspect/render/bounded-edit route with pre-WASM byte budgets, page/object/image/render budgets, atomic distinct-path output, and no fallback. ReportLab, pdfplumber, pypdf, optional specialist PyMuPDF, Poppler, qpdf, pyHanko, veraPDF, Tesseract, and planned pikepdf/OCRmyPDF remain explicit task routes. No lifecycle installer or global mutation is used. |
| Legacy options | done | `codec`, `allowLossy`, `preferNative`, and `relativeDateAsOf` are rejected explicitly. |
| Opaque Office preservation | done | Imported unmodeled parts are content-type/hash/source bound. Unchanged content is preserved; unsupported edits and missing source snapshots fail closed. |
| Low-level OOXML inspect/patch | done | Explicit, bounded package inspection and patching remain available and are never invoked automatically as a fallback. |
| Wire protocol | done | `open_office.artifact.v1`, protocol version 2; removed `allow_lossy` field name and number are reserved. |
| JavaScript source layering | partial | `src/index.mjs` is now a 49-line composition root: it preserves the 42-symbol public API, cross-format kind inference, verify/render/visual-QA/help wrappers, exact model binding re-exports, and five font-metric exports while the complete PDF, Document, Presentation, and Spreadsheet model/layout/render/file-facade domains live in dependency-leaf modules. Visual QA receives artifact-kind inference through one explicit factory boundary, so it has no model back-edge or circular import. Both OpenChestnut Office adapters import their Presentation or Spreadsheet leaf directly instead of creating root-entry back-edges. The shared OOXML module owns JSZip package loading, safety budgets, OPC paths/content types/relationships, recipes/source references, validation, and transactional regeneration behind four internal exports. Root PDF, Document, all eight Presentation, and all five Spreadsheet bindings are strict-identical leaf re-exports, and the cross-format ID allocator remains a single shared module. The root remains 49 lines; future decomposition concerns large but cohesive domain leaves, not the public composition root. |
| Font inventory and design metrics | done | `document.fontFamilies`, `workbook.fontFamilies`, and `presentation.fontFamilies` return fresh, sorted, case-insensitively deduplicated inventories of the model's relevant theme/default/explicit fonts. The public metric registry supports replacement, last-in scoped registration with idempotent disposal, primary-family/style/nearest-weight resolution, clearing, and deterministic Skia subpixel-baseline compensation; invalid metric records are ignored and unsupported family fallback is never silent. |
| Agent black-box PromptBench | partial | The repository-only v1 suite defines 29 realistic prompts (19 PDF, 10 Office), isolated candidate/reference Skill trials against the same packed npm candidate, dirty-worktree plus tarball provenance, immutable input fingerprints including directory trees, exact deliverables, fail-closed outcomes, and generic hard gates. The nine ready cases comprise seven PDF cases, one generated XLSX direct-threaded-reply/resolve workflow, and one generated DOCX classic-comment text-only workflow. The XLSX grader decodes the package to require original comment/person/date/text/order preservation, one GUID-backed direct root-child reply, resolved state, no classic-note downgrade, byte-bound audit/explicit OpenChestnut rewrite evidence, second import, and LibreOffice/Poppler rendering. The DOCX grader independently requires same native classic-comment identity/metadata, all three anchor markers, unchanged body text, no modern/reply graph, a byte-bound public OpenChestnut rewrite audit, second import, and stable LibreOffice/Poppler body render. One clean XLSX candidate trial at `d558a92` and one dirty-worktree DOCX candidate trial at `c0be338` each passed `100/100` across all four categories; neither is a repeat matrix or reference-Skill comparison, and the DOCX result needs a clean-commit repeat before release evidence. All seven ready PDF cases retain independent structural/semantic evidence, Poppler/Pillow all-page comparison where applicable, residue/revision/path-safety or accessibility-claim gates, audit provenance, and agent trace-discipline grading. The attachment fixed matrix passes candidate `3/3` at 100/100 and reference `2/3`; AcroForm similarly passes `3/3 + 2/3`, while active-content, greenfield accessibility, and merge/reorder/selective-watermark each pass `3/3 + 3/3`. The 20 corpus/PKI cases await pinned licensed assets. |

## Spreadsheets

| Capability | Status | Notes |
| --- | --- | --- |
| Cells and scalar values | done | Text, number, boolean, blank, error, formulas, cached values, and Date-to-Excel-serial conversion. |
| Range Quick API | done | A1/R1C1 formulas, stored/projected formula evidence, anchor-sized block writes, values/formulas/all copy with relative translation and even tiling, clear modes, used/current regions, bounded navigation aliases, and scalar/matrix number formats. The shipped workflow exercises these through canonical OpenChestnut export/import/edit/re-export. |
| Formula calculation | partial | JavaScript model evaluates the bounded catalog, including PMT/IPMT/PPMT/CUMIPMT/CUMPRINC/PV/FV/NPER/RATE, SLN/DB/DDB, and NPV/MIRR/XNPV/IRR/XIRR under a finite financial profile. PMT/IPMT/PPMT, cumulative loan, inverse-payment, and depreciation formulas retain their bounded contracts; cash-flow vectors accept at most 10,000 finite values and all rates must be `> -1`. MIRR requires both cash-flow signs plus explicit finance and reinvestment rates, discounts negative flows at the finance rate, and compounds positive flows to the final period at the reinvestment rate. X variants require aligned valid dates; IRR/XIRR use bounded convergence. The native Spreadsheet Skill ships separate three-sheet financial-returns, loan-amortization, and asset-depreciation workflows/fixtures with visible assumptions, guarded sign/reconciliation/payment-identity/rate/PV/FV/NPER-inverse/CUMIPMT/CUMPRINC/MIRR/salvage/roll-forward checks, actual `Date` inputs where relevant, SVG review, and canonical OpenChestnut roundtrips. Invalid inputs fail as `#VALUE!`/`#NUM!`; OpenChestnut writes/reads formulas and honors the XLSX export `recalculate` option. Unsupported Excel functions remain host-calculated or explicit model errors. |
| Static cell styles | done | Number format, font, fill, border, alignment, and protection within the modeled profile. |
| Geometry and views | done | Merges, row heights, column widths, hidden state, and frozen rows/columns. |
| Tables | done | Basic fixed-range worksheet tables and styles. |
| Images | done | Embedded PNG/JPEG worksheet images within the bounded anchor profile. |
| Charts | done | Source-free and recognized imported bar, line, pie, standard area, 50%-hole doughnut, marker-only scatter, and bounded 2D bubble charts with bounded series/title/legend/axis behavior. Scatter owns per-series numeric `xValues/xFormula` plus Y values/formula and two numeric value axes; it is never flattened into a category line chart. Bubble owns aligned positive per-series `xValues/xFormula`, Y `values/formula`, and `bubbleSizes/bubbleSizeFormula`, is never flattened into a category chart, and has an exact three-column `X | Y | Size` range shortcut; multi-series bubble is explicit configuration only. OpenChestnut writes a native no-fill outline on every marker-only scatter series so LibreOffice and Office hosts do not synthesize connecting lines; series-level `line`/`stroke` is rejected and marker borders use `marker.line`. Formula-only internal bindings resolve live category or numeric X/Y/Size caches for inspect, type-aware SVG preview, and OpenChestnut export. Scatter line/smooth variants, bubble 3D/negative/custom-scale/non-area-size profiles, stacked/percent-stacked area, non-50% doughnut geometry, point explosions, and other unmodeled plot options remain source-bound/read-only. |
| Sparklines | partial | Standard Office 2010 `x14:sparklineGroups` line, column, and stacked profiles support source-free authoring, semantic import, fixed-topology property edits, second export/import, inspect, and per-target SVG preview. The public two-range model accepts only reversible one-dimensional target mappings; non-contiguous or otherwise non-reversible native groups remain opaque/source-bound and unchanged. |
| What-If data tables | partial | `sheet.dataTables.add(range, { rowInput, columnInput })` authors canonical native one-variable row/column and two-variable `<f t="dataTable">` formulas through OpenChestnut; semantic import, defensive definition inspection, ordinary surrounding-cell edits, second export/import, source-free bounds/overlap checks, and advanced-attribute preservation are tested. Excel/LibreOffice/native hosts calculate result values. Imported topology/input/orientation mutation and noncanonical graphs remain source-bound/read-only and fail closed. |
| Data validation | partial | Basic list, whole-number, decimal, date, time, and text-length profiles; complex extension graphs remain source-bound. |
| Conditional formatting | partial | Basic cell/value/formula/color-scale style profiles are represented by the public model. For a modeled solid differential fill, OpenChestnut writes the RGB color to both DXF foreground and background solely for native LibreOffice print/render compatibility, then normalizes the imported public model back to one semantic fill color. Complex differential-style and extension graphs remain source-bound. |
| Threaded comments | partial | One root plus multiple direct replies per modeled thread support source-free authoring, native GUID/person/date/done metadata, semantic import, text/state edits, and second OpenChestnut export/import. Reply-of-reply/branched graphs, mentions, orphan parents, cross-cell parents, and unsupported identity extensions remain opaque/source-bound and fail closed on replacement. |
| Pivots, QueryTables, connections, dynamic arrays | partial | Existing imported objects can remain opaque and unchanged. Source-free authoring or semantic editing is not part of the 0.2 facade boundary. |

## Documents

| Capability | Status | Notes |
| --- | --- | --- |
| Paragraphs and runs | done | Ordered runs, bold/italic/underline, fonts, sizes, colors, and paragraph alignment/spacing/indent within the modeled profile. |
| Styles | done | Document defaults plus paragraph/character styles and `basedOn` relationships within the bounded graph. |
| Sections and pages | done | Page size, orientation, margins, section breaks, and section-level first/even behavior. |
| Headers and footers | done | Default/first/even references, section scoping, text, and PAGE/simple field content within the supported topology. |
| Lists | partial | Numbered and character-bulleted lists in the bounded numbering profile. Picture bullets and complex inherited numbering graphs remain source-bound. |
| Tables | done | Fixed geometry, rectangular cell topology, text, merges, widths, `TableGrid`, and bounded direct formatting. Arbitrary source-free custom table-style graphs are not materialized; topology-changing edits to imported complex tables fail. |
| Links and fields | partial | External/internal links and the bounded PAGE/NUMPAGES/SECTION/date/property `w:fldSimple` catalog are editable. Paragraph runs may author/import canonical five-run `SEQ <label> \\* ARABIC`, `REF <bookmark> \\h`, and `PAGEREF <bookmark> \\h` fields; a source-free SEQ may wrap only its cached number in one native bookmark for real caption cross-reference targets. `document.materializeFields()` transactionally computes bounded SEQ counters and REF cached text with dry-run evidence; missing targets fail before mutation. PAGEREF/page fields still require a real pagination host. Imported field positions, instructions, and bookmark identity remain source-bound while caches and ordinary text are editable. Source-free documents may also author one canonical one-paragraph complex TOC placeholder with bounded heading-level switches and an explicit update-on-open hint. Refreshed cross-paragraph TOCs and other field graphs remain explicit package workflows or opaque/read-only. |
| Bookmarks | partial | Source-free bookmarks around exactly one paragraph-like block and narrow SEQ cached-result bookmarks share one case-insensitive name/native-ID space, author paired native `w:bookmarkStart`/`w:bookmarkEnd`, and round-trip semantically. Recognized imported whole-block bookmarks remain read-only; recognized inline-field bookmarks retain fixed source-bound topology and identity while cached text is editable. The two bounded profiles cannot nest in one paragraph. Cross-block, nested, crossing, table-cell, and otherwise complex graphs remain opaque-preserved and fail closed on mutation. |
| Footnotes and endnotes | partial | Source-free plain-text notes at the end of one paragraph or list item author native `word/footnotes.xml` / `word/endnotes.xml` parts, required separator entries, and body references. Recognized canonical imports expose inspect/resolve anchors and permit text-only body edits; kind, anchor, native ID, and topology remain source-bound. Multi-paragraph bodies, reused references, custom note graphs, anchor movement, and more than one bounded note per target remain opaque-preserved and fail closed. |
| Images | done | Embedded PNG/JPEG inline images with bounded geometry and alternative text. |
| Comments | partial | Classic comments and bounded anchors; modern reply/presence graphs are preserved unchanged but not newly authored. |
| Tracked changes | partial | Source-free and recognized imported whole-paragraph `w:ins`/`w:del` revisions with exactly one text run support authoring, semantic import, and fixed-topology text/author/date edits. Native revision IDs and unmodeled formatting remain source-bound. In-paragraph replacements, mixed accepted/revision runs, nested revisions, moves, property changes, and irregular graphs remain opaque/source-bound and read-only. The `trackRevisions` setting for automatically tracking future edits is still unsupported and fails closed. |
| Inline plain-text content controls | partial | Source-free body-paragraph runs author canonical plain-text `w:sdt` controls with model ID, tag, alias, deterministic native `w:id`, visible text, and bounded run formatting. Recognized canonical imports expose inspect/resolve handles and transactional strict fill-by-tag; text/tag/alias edits are permitted while native identity and run topology remain source-bound. Rich, block, cell, nested, data-bound, dropdown, date, checkbox, placeholder-document, locked, header/footer, and extension-bearing controls remain opaque/source-bound and fail closed on semantic replacement. |
| Bibliography and citations | partial | Source-free documents author one canonical `b:Sources` Custom XML catalog plus whole-paragraph `w:fldSimple` `CITATION <tag>` fields. Ordinary Author name lists or one corporate author, supported scalar source fields, catalog settings, and multiple sources round-trip; recognized imports permit source content and citation display edits while source order/IDs/tags and citation tags/topology remain source-bound. Other contributor roles, complex switches/results, multiple/irregular catalogs, bibliography output fields, and extension graphs remain opaque/read-only. |
| Advanced settings, complex fields, drawings | partial | Preserve imported content when source evidence remains valid; unsupported creation/editing fails closed. Modeled section/header behavior, `evenAndOddHeaders`, and the `updateFields` refresh hint are writable; the hint never asserts that cached results are current. `trackRevisions`, `mirrorMargins`, and `documentProtection` fail closed. Complex fields outside the canonical unrefreshed TOC placeholder remain source-bound. |

## Presentations

| Capability | Status | Notes |
| --- | --- | --- |
| Slides and basic shapes | done | Text boxes, rectangles/ellipses, and `roundRect` with bounded transforms; source-free slides append or insert after a Slide/0-based index (or first with `after: null`) transactionally. `slide.moveTo(index)` rewrites only the retained imported `SlidePart` order in `p:sldIdLst`. `slide.delete()` removes a source-free slide, and performs a real imported OPC delete only for an isolated layout-only SlidePart with no inbound relationship or presentation identity extension; its part and relationship part disappear while retained slide XML stays byte-identical. `slide.duplicate()` is separately bounded: an unchanged original imported slide containing canonical shapes, canonical inline fixed-grid tables, canonical embedded rectangular images, and recursively canonical groups whose descendants contain only those same leaf kinds, exactly one layout relation, only picture-bound image relations, and optionally one closed `NotesSlide -> NotesMaster` / back-to-source-slide leaf plus one canonical legacy `SlideCommentsPart` leaf becomes a distinct SlidePart and new presentation relationship. Accepted tables are inline-only and cannot add a fill, link, or other package edge. Accepted groups add no relationship themselves, and every nested picture must consume one exact verified ImagePart relationship. The operation deliberately shares the verified layout, immutable ImageParts, NotesMaster, and one immutable presentation-wide `CommentAuthorsPart`; accepted NotesSlide and SlideComments XML are copied byte-for-byte, and only the notes back-reference is repointed at the clone while origin bytes remain intact. The clone cannot be edited or replace its origin until a successful export/reimport; supported legacy comments then remain source-bound read-only. Imported adds, repeat/mutated clones, and broad clones or deletes involving rich/connected comments, connectors, charts, OLE, hyperlinks, external/unbound/irregular images, custom shows, sections, extensions, or other graph edges fail closed. |
| Fill, line, shadow | done | Basic solid/no-fill, line color/width/dash, and bounded outer shadow. Imported presence-aware `p:sp/@useBgFill` is exposed read-only as `shape.useBackgroundFill`, affects SVG preview paint, and is byte-preserved unchanged; source-free authoring or semantic mutation fails closed. Complex theme/effect graphs remain source-bound. |
| Rich text and lists | done | Paragraphs, runs, common formatting, bullets/numbering, and bounded links. |
| Connectors | done | Straight and polyline connectors with bounded line/arrow styling. |
| Groups | partial | Source-free recursive DrawingML `p:grpSp` trees support outer `off/ext`, signed local `chOff/chExt`, and ordered modeled shapes, connectors, embedded images, fixed-grid tables, literal bar/line/pie charts, the same bounded combo profile (including its canonical secondary-line axis pair), and nested groups. Canonical imported groups support semantic import plus fixed-topology group/descendant edits and second import. Group-level fill/effect/lock/rotation/flip/extension semantics or any unsupported descendant keep the entire group opaque, source-bound, and read-only. |
| Literal custom geometry | partial | Source-free and recognized imported DrawingML `custGeom` paths support bounded literal move, line, cubic Bézier, and close commands. Guides, handles, connection sites, arcs, quadratic curves, text rectangles, and per-path paint overrides remain source-bound. |
| Images | done | Embedded PNG/JPEG/GIF/safe-SVG images support bounded rectangular placement, alternative text, rotation/flips, `stretch`/`cover`/`contain`, and explicit signed crop through native DrawingML `a:srcRect`. Recognized source-bound source rectangles support semantic import plus add/edit/remove; import reports `fit: "stretch"` with explicit crop because PPTX has no native fit keyword. External links, non-rectangular masks, border radius, blip effects, and unsupported transforms remain opaque-preserved and fail closed on mutation. |
| Tables | done | Source-free fixed rectangular tables and fixed-topology imported cell text/geometry edits. |
| Charts | done | Source-free bar, line, and pie charts plus a bounded literal clustered bar+line combo profile. A combo requires at least one primary bar and one line series; its lines are either all primary (one shared category/value pair) or all secondary (one canonical top/right category/value pair under `axes.secondary`). Chart-level data labels are duplicated across its two plots, and plot/series/point topology stays fixed after import. External data, mixed primary/secondary line groups, secondary bars, per-series labels/point overrides, smooth lines, trendlines, error bars, and irregular combo graphs remain source-bound or fail closed. |
| Secondary-axis combo host rendering | partial | The canonical top/right profile is Office 2021 schema-validated and covers source-free authoring, import, fixed-topology edit, second import, and explicit `crosses="max"` right-axis semantics. The bundled LibreOfficeDev 26.8 alpha renders the series and right-axis title but locally overlaps dual-value tick labels for cache-only PPTX charts; that host-specific visual placement is not claimed as passed and requires a Microsoft PowerPoint/native-host QA lane before release-grade visual fidelity. |
| Slide backgrounds | partial | Source-free and recognized imported direct slide backgrounds support six-digit RGB/theme solid fills and native style references. Imported direct backgrounds are semantic-hash-bound and may be added, edited, or removed without flattening Layout/Master inheritance; gradients, patterns, images, transforms, effects, and irregular graphs remain opaque-preserved and fail closed on mutation. |
| Speaker notes | partial | Source-free plain-text notes author a canonical Notes Master/Notes Slide graph. Imported notes expose stable inspect/resolve anchors; a single bounded body placeholder is hash-bound and editable in place, while rich/irregular notes remain opaque-preserved and fail closed on mutation. Source-bound slides without a notes part cannot add one in place. The narrow imported-slide clone profile may carry one unchanged closed `NotesSlide -> NotesMaster` / back-to-slide leaf by byte-copying the NotesSlide and pointing its clone-local relationship back to the new SlidePart; it does not make notes editable before export/reimport or permit arbitrary notes relationships. |
| Legacy comments | partial | Source-free `slide.comments.addThread(undefined, text, { author, position, created? })` authors one standard legacy `p:cm` with an explicit slide coordinate, one author, and one text item. Recognized legacy comments import for inspect/resolve and are byte-preserved only when unchanged. The narrow imported-slide clone profile may carry one unchanged canonical `SlideCommentsPart` by copying its XML byte-for-byte and reusing the verified immutable presentation-wide `CommentAuthorsPart`; the comments part and author catalog must have no connected relationship graph, and comments remain read-only before and after the export/reimport boundary. Replies, resolved/reopened state, reactions, element/text anchors, and all modern comment graphs remain opaque/source-bound and fail closed rather than being flattened into legacy markup. |
| Master/Layout fidelity | partial | Source-free authoring supports exactly one canonical master plus `blank`, `title`, `titleOnly`, or `obj` layouts, direct RGB/theme backgrounds, bounded master text styles, and direct-frame `title`/`body`/`ctrTitle`/`subTitle` text placeholders. `layout.placeholders.summary()` offers a defensive discovery snapshot without exposing mutable model references. `slides.add({ layout })` transactionally binds and materializes matching direct-frame placeholder shapes; same-layout reapplication is idempotent while a materialized layout cannot be switched. Export writes native `p:ph` identities and imports them semantically. Multiple masters, master-theme overrides, non-text placeholder classes, inherited source-free geometry, arbitrary template graphs, and every imported Master/Layout semantic mutation remain source-bound/read-only and fail closed. |
| Presentation view metadata | partial | `presentation.view` provides reference-compatible local gridline/guide visibility controls. OpenChestnut imports grid spacing, snap flags, and horizontal/vertical guides from `ppt/viewProps.xml`; layouts/masters expose frozen `slideGuides`, unchanged exports retain the native part byte-for-byte, and source-free authoring or semantic mutation fails closed. Other view modes and extension graphs remain opaque-preserved. |
| OLE, SmartArt, media, 3D, custom XML | partial | An eligible imported top-level OLE object with exactly one internal, uniquely bound, unshared XLSX package exposes defensive workbook extraction and payload-only replacement. The replacement is copied, limited to 1–16 MiB, bound to source hash/content type/relationship identity, opened by the Microsoft Open XML SDK, and Office 2021 validated; the OLE shell, preview, relationship topology, source bytes, and every unrelated package part remain unchanged. Shared, external, ambiguous, non-XLSX, source-free, or otherwise irregular graphs remain opaque-preserved and read-only; unsupported mutation fails closed without reconstruction or fallback. |

## PDF

| Capability | Status | Notes |
| --- | --- | --- |
| Greenfield creation and trusted-model roundtrip | done | `PdfArtifact`/`PdfFile` provide multi-page semantic/tagged authoring and clean-room model roundtrip. ReportLab is an optional layout-oriented greenfield provider through the shipped Skill script. |
| Arbitrary-file reading and extraction | done | MuPDF.js is the required default parser and supplies structured text geometry, raster-placement data, links, annotations, and widgets under explicit budgets. PDF.js, pdfplumber, pypdf, PyMuPDF, pdfinfo, and Poppler retain independent specialist roles. Parser reconstruction is evidence for inspect/QA, never an edit representation. |
| Embedded attachment quarantine | done | The shipped typed pypdf read-only primitive inventories document-level and page-level FileSpecs, preserves duplicate/raw identity and MIME evidence, confines Unicode/cross-platform names, enforces decoded count/per-file/total budgets, transactionally extracts without opening payloads, verifies every SHA-256, and proves source immutability. Malformed FileSpecs, stream failures, path escape, collisions that cannot be represented safely, and budget exhaustion fail closed without partial output. |
| Text and tables | done | Positioned/flow text, extraction, modeled tables, spans, headers, and geometry. |
| Images and charts | done | PNG/JPEG images, vector charts, alternative text, and extraction/model records. |
| Page geometry and reading order | done | Stable IDs, explicit logical reading order, inspect, resolve, and layout JSON. |
| Accessibility | partial | Tagged structure, H1-H6 headings, language/title, table semantics including constrained consecutive-page logical tables, figures/alt text, meaningful tagged Link annotations with OBJR, explicit reading order, and artifact-marked running text. The six-page CJK Skill example separates modeled verification, optional veraPDF machine evidence, and required human PDF/UA judgment; this is not a claim of full PDF/UA conformance. |
| Render and visual QA | done | SVG/layout preview plus required MuPDF.js PNG/JPEG native rendering under a preallocation pixel budget; independent Poppler/Playwright/sharp/canvas render paths and visual comparison remain available. |
| Existing-PDF native editing | partial | MuPDF.js directly edits original bytes for bounded text annotations, text/choice/checkbox fields, page deletion/reordering, metadata, embedded-file/link deletion, and rewrite or byte-prefix-verified incremental saves. Redaction and delete operations require rewrite because incremental revisions retain old content; radio export mapping, unsupported operations, signed incremental changes, and unsafe modes fail closed. The specialist PyMuPDF path retains positioned-text/image/replacement workflows; pypdf provides appearance-aware forms and complete-source merge/reorder/selective watermarking. General Word-style reflow, dynamic XFA, complex Acrobat JavaScript, 3D, and RichMedia remain outside the universal contract. |
| Redaction and sanitize | partial | MuPDF.js applies real text/rectangle redactions only in a rewrite and explicitly labels that result as narrower than sanitize. The specialist PyMuPDF workflow adds active-content cleanup, strict scrub, full rewrite, original-prefix removal, decoded/raw/metadata/attachment/annotation/form/action/hidden-text/OCR residue scans, and single-revision checks. Image-bearing high-trust delivery still requires separately installed Tesseract. |
| Signatures and conformance | partial | Signature dictionaries are detected from the PDF object graph rather than raw-text regex. MuPDF.js rejects signed incremental edits and requires explicit invalidation for a signed rewrite, while reporting trust as unknown. Creation/trust/LTV/DocMDP validation remains an external pyHanko workflow; PDF/A/UA machine validation remains external veraPDF. |

## Reference Skills

The published layout is five native plugin bundles and six Skills; `test/skill-harness` is development-only and is not a public Skill surface. Detailed evidence and gaps are in [reference Skill compatibility](reference-skills.md).

| Skill | Status | Main workflow |
| --- | --- | --- |
| Documents | partial | Native packaging plus the ordinary public-API create/import/edit/export workflow are runnable and tested through canonical OpenChestnut, semantic assertions, and real LibreOffice page QA. A shipped bounded workflow also updates exactly one uniquely located imported classic comment's text while preserving its native ID, author, initials, timestamp, target and three XML anchor markers, then reimports, renders, and emits a byte-bound audit; modern comments/replies, resolved state, presence metadata, and ambiguous or topology-changing edits fail closed. Bounded whole-block bookmarks/internal hyperlinks, plain-text footnotes/endnotes, whole-paragraph tracked insertions/deletions, inline plain-text content controls with strict fill-by-tag, canonical inline SEQ/REF/PAGEREF runs with optional caption-number bookmarks and transactional SEQ/REF cache materialization, bibliography-backed citations, and native TOC placeholders with update-on-open intent cross the public model/protobuf/Open XML path. PAGEREF/page calculation, refreshed cross-paragraph TOCs, complex bibliography/field/bookmark/note/revision graphs, rich/block/cell/data-bound/dropdown/date/checkbox SDTs remain explicit source-bound or advanced host/package workflows. |
| Spreadsheets | partial | The native plugin, reference-style core workbook example, shipped Range/R1C1, financial-returns, loan-amortization, asset-depreciation, seven-family chart, standard-sparkline, and What-If data-table workflows, and independent three-sheet operating-forecast forward test pass canonical XLSX export/import/edit/re-export plus semantic/render verification. The financial workflows place assumptions, standard NPV/XNPV/IRR/XIRR/MIRR/PMT outputs, formula-driven PMT/IPMT/PPMT schedules with RATE/PV/FV/NPER inverse audits plus CUMIPMT/CUMPRINC schedule reconciliations, or SLN/DB/DDB fixed-asset schedules with salvage/basis checks, and finance-color conventions in separate auditable sheets. The named high-value Range Quick API slice, direct `thread.addReply()` comment workflow, formula-only internal chart binding, bar/line/pie/standard-area/50%-doughnut/marker-scatter/2D-bubble charts, canonical Office 2010 line/column/stacked sparklines, and bounded one-/two-variable native data tables are compatible; nested/mention comment graphs, additional chart families and advanced plot options, imported data-table topology mutation, and other extended reference surfaces remain incomplete. |
| Excel live control | partial | Native routing Skill and connector declaration are present; execution depends on a host-provided connected Excel session outside the npm package. |
| Presentations | partial | The native plugin and complete 26-slide built-in template pass canonical OpenChestnut export/import, including bounded custom geometry. Direct solid/style-reference slide backgrounds, a source-free one-master/direct-frame-text-placeholder layout slice, plain-text speaker notes, embedded rectangular image `stretch`/`cover`/`contain` plus signed crop, recursive native `p:grpSp` groups, payload-only replacement of one eligible imported embedded XLSX, imported-deck `slide.moveTo(index)`, real `slide.delete()` for an isolated layout-only source SlidePart, and `slide.duplicate()` for one unchanged canonical shape/inline-table/embedded-image/recursive-group layout leaf with optional closed notes and canonical legacy-comments leaves cross the public Skill/model/protobuf/Open XML path with inspect/resolve evidence, real render-backed QA, source/hash/relationship binding, retained-origin byte preservation, fixed-topology imported edits, and fail-closed advanced-graph preservation. Accepted clone tables are inline-only and add no package relationship; accepted groups add no relationship themselves, and every nested picture must consume one exact verified ImagePart relationship. Clone creates an independent SlidePart; clone-local image relations share verified immutable ImageParts, accepted NotesSlide XML is copied byte-for-byte and shares its verified NotesMaster/back-reference pattern, and accepted SlideComments XML is copied byte-for-byte while sharing the immutable presentation-wide CommentAuthorsPart. It must be exported/reimported before editing; supported legacy comments remain source-bound read-only, while rich/connected comments, connector/chart/OLE/hyperlink/custom-show/section/extension/external-or-irregular-image graphs and all broad clone/delete operations still need an explicit OPC relationship-graph operation. The broader reference API guide still exceeds the current PPTX boundary. |
| PDF | partial | The native plugin is a reference-compatible capability-router superset: greenfield `PdfArtifact`; required lazy MuPDF.js arbitrary-file import/inspect/render/bounded edits; a runnable `scripts/mupdf.mjs` CLI; ReportLab/pdfplumber/pypdf specialist workflows; typed attachment quarantine and AcroForms; merge/reorder/selective watermarking; specialist PyMuPDF sanitize; explicit save policies; canonical audits; residue scans; and independent Poppler comparison. Fixed evaluation matrices retain their recorded results. qpdf/pyHanko/veraPDF external execution, OCRmyPDF/pikepdf adapters, hosted specialist-provider tests, remaining matrices, and broader corpus QA remain open. |
| Template Creator | done | The reference-compatible local utility creates or explicitly updates same-kind DOCX/PPTX/XLSX-backed `artifact-template-*` Skills under a configurable private home. It retains reference/preview bytes, validates PNG chunks/CRCs, enforces file budgets, rejects symlink-bearing update trees, prevents concurrent writers, recovers interrupted updates, preserves extra owned files, and verifies transactional cleanup. The release pack gate also executes the creator from a production-only clean install of the actual npm tarball and verifies its generated Skill and retained bytes. |
