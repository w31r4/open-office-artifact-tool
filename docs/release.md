# Release readiness

Status date: 2026-07-16

## Current package

- Package: `open-office-artifact-tool`
- Version: `0.1.0`
- Publish state: publish-ready, not published from this environment

## Required local gates

The current release gate is:

```sh
npm run proto:check
npm run build:open-chestnut
npm run verify:open-chestnut-build
npm run test:open-chestnut-dotnet
npm test
npm run docs:api
npm run test:pack
dotnet test native/OfficeBridge # when dotnet is installed
npm run test:playwright-renderer # when Playwright/Chromium are installed
```

Use the consolidated checker:

```sh
npm run release:check
```

The checker runs npm tests/docs/pack, conditionally runs dotnet tests when `dotnet` is available, audits every locked dependency license against `scripts/license-policy.json` and `THIRD_PARTY_NOTICES.md`, checks npm auth, checks the published npm version, and reports blockers.

GitHub CI and the manual release workflow install Playwright Chromium, LibreOffice Writer/Calc/Impress, Poppler, and .NET 8 before running these gates. Their environment-probe step is required, so hosted runs exercise real browser and native render branches instead of silently accepting skips.

The package publishes `codecs/open-chestnut` as the canonical Office codec. `codecs/openxml-wasm` and the old npm script names remain deprecated compatibility aliases and are covered by source-tree and clean-install tests; all runtime assets, C# projects, CI commands, fixtures, and new documentation use OpenChestnut.

`test/package-contents.mjs` validates the actual dry-run tarball manifest. It requires the project-local runnable skills, optional native Office bridge source, public protobuf source/generated binding, runtime integrity manifest/SBOM/.NET notices, consumer API docs, format codecs and validators, published fixtures, shared visual-baseline lifecycle, and third-party notices while rejecting reference/handoff material, development tests, local dotnet `bin/obj` output, repository-only planning notes, OpenChestnut C# source, and development build scripts. The GitHub source checkout remains the authoritative location for C# projects, locked dependencies, build tooling, tests, and same-host reproducibility verification; excluding their second copy from npm does not change any export or runtime lookup. The current chart-series-line consumer package contains 153 files, is 4,837,162 bytes compressed and 16,258,847 bytes unpacked. It remains below the unchanged 8,000,000/16,700,000-byte gates with 441,153 bytes of unpacked headroom, and the no-local-dotnet clean-install gate passes. Exact-SHA hosted run [`29408434715`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29408434715) verifies the same 153-file clean install, complete source-checkout reproducibility, real Chromium/LibreOffice 24.2.7.2/Poppler 24.02.0 tooling, generated protocol/API checks, OfficeBridge 5/5, OpenChestnut 133/133, and two byte-identical 38-file/13,584,062-byte Linux runtime builds. Most of the footprint remains the .NET/Open XML SDK runtime required for no-local-dotnet Office I/O.

The current chart-series-marker increment produces a 154-file consumer package of 4,845,101 bytes compressed and 16,282,229 bytes unpacked, leaving 417,771 bytes below the unchanged unpacked-size gate. The no-local-dotnet clean-install probe passes. Its source-built macOS runtime contains 38 files/13,593,788 bytes, and two isolated builds are byte-identical across all 39 audited files. OpenChestnut Release tests pass 134/134 and OfficeBridge passes 5/5. The required `open-chestnut-basic` gate crosses diamond/8 authoring and triangle/10 source-bound editing through two WASM passes; all five model sheets pass Chromium 149.0.7827.55, and all four native pages pass LibreOfficeDev 26.8.0.0.alpha0 plus Poppler 26.05.0. Exact-SHA hosted Linux run [`29410193061`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29410193061) passes the same 154-file clean install, all npm/skill/render/docs/package gates, real LibreOffice 24.2.7.2/Poppler 24.02.0, OpenChestnut 134/134, OfficeBridge 5/5, and two byte-identical 38-file/13,593,790-byte runtime builds.

The current chart-level line-smoothing increment produces a 155-file consumer package of 4,847,772 bytes compressed and 16,292,979 bytes unpacked, leaving 407,021 bytes below the unchanged unpacked-size gate. The no-local-dotnet clean-install probe passes. Its source-built macOS runtime contains 38 files/13,597,884 bytes, and two isolated builds are byte-identical across all 39 audited files. OpenChestnut Release tests pass 135/135 and OfficeBridge passes 5/5. The required `open-chestnut-basic` gate crosses chart-level smooth `true` authoring and explicit `false` source-bound editing through two WASM passes; all five model sheets pass Chromium 149.0.7827.55, and all four native pages pass LibreOfficeDev 26.8.0.0.alpha0 plus Poppler 26.05.0. Exact-SHA hosted Linux run [`29411788218`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29411788218) passes the same full npm/skill/render/docs/package gate with real LibreOffice 24.2.7.2/Poppler 24.02.0, OpenChestnut 135/135, OfficeBridge 5/5, the 155-file clean install, and two byte-identical 38-file/13,597,886-byte runtime builds.

The current chart-level line-grouping increment keeps the consumer package at 155 files, grows it to 4,849,498 bytes compressed and 16,301,411 bytes unpacked, and leaves 398,589 bytes below the unchanged unpacked-size gate. The no-local-dotnet clean-install probe passes. Its source-built macOS runtime contains 38 files/13,600,956 bytes, and two isolated builds are byte-identical across all 39 audited files. OpenChestnut Release tests pass 135/135 and OfficeBridge passes 5/5. The required `open-chestnut-basic` gate crosses `stacked` authoring to `percentStacked` source-bound editing alongside smooth true→false through two WASM passes; all five model sheets pass Chromium 149.0.7827.55, and all four native pages pass LibreOfficeDev 26.8.0.0.alpha0 plus Poppler 26.05.0. Exact-SHA hosted Linux run [`29413451804`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29413451804) passes the same full npm/skill/render/docs/package gate with real LibreOffice 24.2.7.2/Poppler 24.02.0, the 155-file clean install, OpenChestnut 135/135, OfficeBridge 5/5, and two byte-identical 38-file/13,600,958-byte runtime builds.

The current chart-level line-vary-colors increment keeps the consumer package at 155 files, grows it to 4,850,140 bytes compressed and 16,304,252 bytes unpacked, and leaves 395,748 bytes below the unchanged unpacked-size gate. The no-local-dotnet clean-install probe passes. Its source-built macOS runtime contains 38 files/13,601,980 bytes, and two isolated builds are byte-identical across all 39 audited files. OpenChestnut Release tests pass 135/135 and OfficeBridge passes 5/5. The required `open-chestnut-basic` gate crosses direct vary-colors true authoring to false/node removal alongside stacked→percentStacked and smooth true→false through two WASM passes; all five model sheets pass Chromium 149.0.7827.55, and all four native pages pass LibreOfficeDev 26.8.0.0.alpha0 plus Poppler 26.05.0. Exact-SHA hosted Linux run [`29414913314`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29414913314) passes the same full npm/skill/render/docs/package gate with real LibreOffice 24.2.7.2/Poppler 24.02.0, the 155-file clean install, OpenChestnut 135/135, OfficeBridge 5/5, and two byte-identical 38-file/13,601,982-byte runtime builds.

The current explicit Office codec-policy increment adds the frozen public IDs `javascript` and `open-chestnut` to all six main DOCX/PPTX/XLSX file-facade import/export calls while retaining JavaScript as the compatibility default and isolating the deprecated `openxml-wasm` name to its tested compatibility subpath. The three runnable Office skills and the no-local-dotnet clean-install probe now use those main-facade calls for OpenChestnut roundtrips. Local gates pass all npm/skill/render/help/docs/proto/package tests, OpenChestnut 135/135, OfficeBridge 5/5, and two byte-identical 38-file/13,601,980-byte runtime builds. Real Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0 branches pass. The 156-file consumer package is 4,851,268 bytes compressed and 16,314,479 bytes unpacked, leaving 385,521 bytes below the unchanged unpacked-size gate. Exact-SHA hosted Linux run [`29429961856`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29429961856) passes the same main-facade/runnable-skill implementation with all npm/render/docs/proto/package gates, real LibreOffice 24.2.7.2/Poppler 24.02.0, the 156-file clean install, OpenChestnut 135/135, OfficeBridge 5/5, and two byte-identical 38-file/13,601,982-byte runtime builds.

The current styled line-marker increment keeps symbol/size wire tags intact and adds marker fields 3/4 for direct untinted RGB fill and the reusable bounded RGB/preset-dash/width outline. Both JavaScript and OpenChestnut author/import/render the same `{ symbol, size, fill, line }` projection; source-bound edits include all four semantics in the chart hash, while theme/transformed/gradient/picture marker graphs remain read-only and exact-preserved. The required `open-chestnut-basic` gate crosses a styled diamond to a differently styled triangle through two WASM exports and real model/native QA. Local gates pass all npm/skill/render/help/docs/package tests, OpenChestnut 135/135, OfficeBridge 5/5, real Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 156-file consumer package is 4,852,977 bytes compressed and 16,322,915 bytes unpacked, leaving 377,085 bytes below the unchanged unpacked-size gate. The source-built runtime contains 38 files/13,605,052 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29432070847`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29432070847) passes the same full npm/skill/render/docs/proto/package gate with Chromium 149.0.7827.55, LibreOffice 24.2.7.2, Poppler 24.02.0, the 156-file clean install, OpenChestnut 135/135, OfficeBridge 5/5, and two byte-identical 38-file/13,605,054-byte runtime builds.

The current opaque-OPC graph increment completes the previously declared public `OpaqueOpcPart.content_type` and per-part `relationships` contract. A namespace-aware content-types reader validates every owned or unowned OPC part while correctly excluding the manifest from self-typing; only unowned parts cross the opaque wire, and source-backed bytes remain single-owned by the bounded snapshot. Resolved content type and sorted relationship adjacency now participate in graph comparison, so duplicate/missing declarations and wire metadata tampering fail before mutation. Local OpenChestnut tests pass 140/140 across XLSX malformed/custom-part cases plus DOCX/PPTX content-type coverage, and the bundled-WASM smoke exercises the same custom-part import and tamper rejection. `npm test`, generated API/proto checks, the no-local-dotnet clean install, OfficeBridge 5/5, and real Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0 branches pass. The 156-file package is 4,854,306 bytes compressed and 16,329,549 bytes unpacked, leaving 370,451 bytes below the unchanged gate. The source-built macOS runtime contains 38 files/13,610,684 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29434484910`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29434484910) passes the same npm/skill/render/docs/proto/package gate with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 156-file clean install, OpenChestnut 140/140, OfficeBridge 5/5, and two byte-identical 38-file/13,610,686-byte runtime builds.

The current PPTX native-object graph increment additively projects `native_kind`, namespace-aware relationship references, and bounded reachable part paths while keeping `opaque_opc` as the sole package-byte/relationship owner. C# and bundled-WASM tests cover Office 2021-valid OLE, four-part SmartArt, recursive contentPart/custom-XML-properties graphs, cycles, missing edges/parts, budget failures, byte-exact preservation, JS fallback export, inspect/resolve metadata, and unsafe JS part paths. The runnable `open-chestnut-preservation` fixture now carries all three native kinds through modeled editing and real presentation QA. Local gates pass `npm test`, generated API/proto checks, the no-local-dotnet clean install, OpenChestnut 142/142, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 158-file package is 4,861,091 bytes compressed and 16,361,626 bytes unpacked, leaving 338,374 bytes below the unchanged 16,700,000-byte gate. The source-built macOS runtime contains 38 files/13,623,484 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29437234633`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29437234633) passes the same complete npm/skill/render/docs/proto/package gate with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 158-file clean install, OpenChestnut 142/142, OfficeBridge 5/5, and two byte-identical 38-file/13,623,486-byte runtime builds.

The current PPTX native-object placement increment keeps the recursive graph wire stable and makes only recognized top-level OLE/diagram GraphicFrames plus contentPart-bearing GroupShapes placement-editable. The public facade exposes `setName(...)` and `setPosition(...)`; C# revalidates editability from the actual source DOM, rejects wire escalation, raw XML/graph tampering, invalid frames, and every topology change, and post-validates the output after scrubbing only the owned root name/frame plus the OLE preview frame. The JavaScript fallback applies the same root rewrite and never touches later nested transforms. The required `open-chestnut-preservation` fixture renames and moves/resizes all three kinds, re-imports their exact frames/editability, preserves every recursive payload byte, and passes package/model/Playwright/LibreOffice/Poppler QA. Local gates pass `npm test`, generated API/proto checks, `npm run test:pack`, OpenChestnut 142/142, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 158-file package is 4,866,988 bytes compressed and 16,375,779 bytes unpacked, leaving 324,221 bytes below the unchanged 16,700,000-byte gate. Two isolated local builds are byte-identical across all 39 audited files around a 38-file/13,627,580-byte runtime. Exact-SHA hosted Linux run [`29439588455`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29439588455) passes the same complete gate with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 158-file clean install, OpenChestnut 142/142, OfficeBridge 5/5, and two byte-identical 38-file/13,627,582-byte runtime builds.

The current PPTX OLE embedded-workbook increment additively introduces the independent field-11 `PresentationOleWorkbook` locator and content-addressed replacement asset without relaxing the rest of the native graph. Only a recognized top-level OLE with one non-shared internal package relationship to one ordinary XLSX part receives `getEmbeddedWorkbook()` / `replaceEmbeddedWorkbook(...)`; both codec paths count inbound relationships across the complete OPC package and reject a workbook target shared within one owner or across slides. OpenChestnut and the JavaScript fallback both validate changed workbook bytes and preserve path, content type, relationship, raw XML, preview image, and every other recursive part. The runnable `open-chestnut-preservation` fixture now replaces a real source workbook, combines the payload edit with placement/master/layout/text changes, re-imports the cell marker, and passes Playwright/LibreOffice/Poppler QA while retaining the source preview. Local gates pass `npm test`, generated API/proto checks, the 159-file no-local-dotnet clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 39-file build audits around a 38-file/13,640,380-byte runtime. The package is 4,877,823 bytes compressed and 16,407,161 bytes unpacked, leaving 292,839 bytes below the unchanged 16,700,000-byte gate. Exact-SHA hosted Linux run [`29443385695`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29443385695) passes the same complete npm/skill/render/docs/proto/package gate at `36c2fdfc6ebc36a26ecd4a33e2d8270e4cf52c3b` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 159-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,640,382-byte runtime builds.

The current XLSX plot-level data-label increment adds independent chart field 16 with presence-aware `showValue`/`showCategoryName` semantics. JavaScript and OpenChestnut author/import the same canonical `c:dLbls` projection, render/inspect it, and permit add/change/remove on recognized source-bound bar/line/pie charts; false standard residual flags survive unrelated owned edits, while series/point overrides, visible unsupported flags, formatting, positioning, leader lines, extensions, duplicates, and unknown children keep the complete chart read-only and exact-preserved. `open-chestnut-basic` crosses value labels to category labels through two WASM passes and model/package/Playwright/LibreOffice/Poppler QA. Local gates pass `npm test`, `npm run docs:api`, `npm run test:pack`, `npm run proto:check`, OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,882,431 bytes compressed and 16,423,324 bytes unpacked, leaving 276,676 bytes below the unchanged 16,700,000-byte gate. The local runtime contains 38 files/13,648,060 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29445489984`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29445489984) passes the same complete gate at `0e9ef96b372279a610cdba895d4b8827145a5f9b` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,648,062-byte runtime builds.

The current XLSX plot-level data-label-position increment additively assigns optional data-label field 3 to an independently designed nine-value semantic enum and maps it to the standard `c:dLblPos` catalog. JavaScript accepts long public names or OOXML short aliases, while both codecs expose canonical long names and distinguish omitted position from explicit `bestFit`. OpenChestnut authors the schema-correct `dLblPos`-before-flags order, includes position presence/value in the source semantic hash, and permits block or position add/change/remove on recognized source-bound bar/line/pie charts. Unknown/duplicate scalar positions and valid per-point position overrides remain outside the bounded model, making the complete chart read-only and exact-preserved rather than flattening the graph. `open-chestnut-basic` crosses value/`outsideEnd` labels to category/`top` labels through two WASM passes and model/package/Playwright/LibreOffice/Poppler QA. Local gates pass `npm test`, `npm run docs:api`, `npm run test:pack`, `npm run proto:check`, OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,885,329 bytes compressed and 16,436,089 bytes unpacked, leaving 263,911 bytes below the unchanged 16,700,000-byte gate. The local runtime contains 38 files/13,653,180 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29447394093`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29447394093) passes the same complete gate at `88f52e03447622b85bc718c44db81f0e43a8d36b` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,653,182-byte runtime builds.

The current XLSX plot-level series-name-label increment additively assigns optional data-label field 4 to `showSeriesName`, preserving absent, explicit false, and explicit true as three independent wire/model states. Both JavaScript codecs and OpenChestnut map it to direct `c:showSerName` in schema order, include presence/value in inspect/SVG and source semantic hashes, and permit add/change/remove without rewriting unowned false residual flags. Series-name text composes deterministically with category/value labels in SVG. Visible legend/percent/bubble flags, per-series/per-point overrides, and rich label formatting remain fail-closed. `open-chestnut-basic` crosses value+series/`outsideEnd` labels to category+explicit-series-false/`top` through two WASM passes and model/package/Playwright/LibreOffice/Poppler QA. Local gates pass `npm test`, `npm run docs:api`, `npm run test:pack`, `npm run proto:check`, OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,886,390 bytes compressed and 16,438,888 bytes unpacked, leaving 261,112 bytes below the unchanged 16,700,000-byte gate. The local runtime contains 38 files/13,654,716 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29448661598`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29448661598) passes the same complete gate at `423dc6a423bf605cb025a802a4157c6041030ddb` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,654,718-byte runtime builds.

The current PPTX direct-background-removal increment keeps the public background message stable and uses the existing source semantic hash to distinguish a recognized direct `p:bg` from its removed state. `PresentationSlideMaster` and `SlideLayoutTemplate` expose `setBackground(...)`, `clearBackground()`, and `effectiveBackground()` so direct XML state is not confused with inherited theme/master rendering. OpenChestnut removes only a fully recognized direct solid/reference background, retains Office 2021 validity, supports adding it back, and rejects explicit clear intent against an unrecognized native background. The required `open-chestnut-preservation` fixture removes both Master and Layout backgrounds while preserving template text, picture bullets, OLE/diagram/contentPart graphs, embedded-workbook replacement, notes, images, charts, and every unowned part before Playwright/LibreOffice/Poppler QA. Local gates pass `npm test`, `npm run docs:api`, `npm run test:pack`, `npm run proto:check`, OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,887,665 bytes compressed and 16,444,503 bytes unpacked, leaving 255,497 bytes below the unchanged 16,700,000-byte gate. The local runtime contains 38 files/13,654,204 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29450481746`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29450481746) passes the same complete gate at `dceaf5898e4b1a001c3b302409f9a71c2f9b39b9` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,654,206-byte runtime builds.

The current PPTX slide-placeholder effective-geometry increment adds independent public `PresentationPlaceholderIdentity` fields for type, unsigned index, and inherited-geometry presence plus a recognized direct-frame slot on slide shapes. OpenChestnut records only native source facts; JavaScript resolves a missing slide frame against the linked Layout by `idx`, carries `slide`/`layout`/`master`/`unresolved` provenance, and reuses the exact source wire so effective geometry is never flattened into slide XML. Direct Layout frames replace the complete Master frame atomically, idx `0` remains valid, type mismatches do not prevent slide→layout matching, and mutation or source-free placeholder authoring fails closed. The runnable presentation fixture proves Layout-derived position/provenance, exact missing `a:xfrm`, model rendering, and real native rendering after two WASM passes. Full local npm/help/docs/protobuf/clean-install gates pass with OpenChestnut 146/146, OfficeBridge 5/5, Playwright 1.61.1/Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file consumer package is 4,898,504 bytes compressed and 16,482,876 bytes unpacked, leaving 217,124 bytes below the unchanged 16,700,000-byte gate. The source-built macOS runtime contains 38 files/13,667,516 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29457786419`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29457786419) passes the same complete gate at `748281cd1ce74c68ea27b376e3cf315405bef79b` with Chromium 149.0.7827.55, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 146/146, OfficeBridge 5/5, and two byte-identical 38-file/13,667,518-byte runtime builds.

The current PPTX slide-placeholder effective-transform increment keeps the public wire source-only: slide shape fields 11/12 carry placeholder identity and a recognized owner-local direct frame, never computed Layout/Master state. JavaScript now projects effective `shape.transform` rotation and presence-aware flips alongside position/provenance, treats every direct slide `a:xfrm` as one atomic frame, and rejects mutation so unchanged OpenChestnut export cannot flatten derived state. The JavaScript codec independently authors/imports bounded center transforms on ordinary Shapes, exposes them through inspect/layout/SVG, and normalizes native placeholder defaults to `obj`/idx `0`; source-free OpenChestnut Shape transforms remain fail-closed. The runnable presentation fixture crosses inherited rotation/flip through two WASM passes and real rendering. Full local npm/help/docs/protobuf/clean-install/reproducibility gates pass with OpenChestnut 146/146, OfficeBridge 5/5, Playwright 1.61.1/Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file consumer package is 4,899,364 bytes compressed and 16,486,645 bytes unpacked, leaving 213,355 bytes below the unchanged 16,700,000-byte gate. The source-built macOS runtime remains 38 files/13,667,516 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29458982343`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29458982343) passes the same complete gate at `a19a4e94a7c166d264ace570fd5d6af107700544` with Chromium 149.0.7827.55, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 146/146, OfficeBridge 5/5, and two byte-identical 38-file/13,667,518-byte runtime builds.

The current PPTX ordinary-Shape-transform increment additively assigns public `PresentationShape.transform` field 13 to an independent presence-aware message: signed rotation and horizontal/vertical flips use fields 1–3, while position/extents remain Shape fields 2–5 and placeholder direct frames retain their separate wire. `PptxShapeTransformCodec` authors source-free rectangle/ellipse transforms and safely imports, changes, or clears recognized source attributes. Exact recognition requires one complete `a:off` followed by one complete `a:ext`, bounded rotation, and no unknown attributes or children; empty wire messages, attributed coordinates, complex native profiles, and out-of-range values fail closed without weakening residual hashes. JavaScript/OpenChestnut source-free and source-bound roundtrips retain explicit false and deletion semantics, and `open-chestnut-preservation` changes a real ordinary Shape transform before Playwright/LibreOffice/Poppler QA. Full local `npm test`, `npm run docs:api`, `npm run proto:check`, `npm run test:pack`, reproducibility, OpenChestnut 148/148, and OfficeBridge 5/5 gates pass. The 160-file package is 4,901,555 bytes compressed and 16,495,082 bytes unpacked, leaving 204,918 bytes below the unchanged 16,700,000-byte gate. The source-built macOS runtime is 38 files/13,673,148 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29460269257`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29460269257) passes the same complete gate at `e30c8b8c9f633f94f217618a06535be23497e621` with LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 148/148, OfficeBridge 5/5, and two byte-identical 38-file/13,673,150-byte runtime builds.

## Latest observed blockers in this environment

- `npm whoami` fails with `ENEEDAUTH`; this machine is not logged in to npm.
- `npm view open-office-artifact-tool version --json` returns `E404`; the package name/version was not visible on npm from this environment.
- `dotnet test native/OfficeBridge` passes locally with .NET 8 (`5` tests passed on macOS arm64); Windows + Microsoft Office automation remains an external integration gate.
- `dotnet test native/OpenChestnut/OpenChestnut.sln --configuration Release` passes locally and on exact-SHA hosted Linux with `140` tests. In addition to the drawing corpus, coverage validates content types for every OPC part, public metadata for every unowned part, per-part relationship adjacency, malformed/duplicate/missing declarations, source-bound exact preservation, and graph-metadata tampering. Local macOS produces 38 runtime files/13,610,684 bytes with two byte-identical 39-file build audits; hosted Linux produces two byte-identical 38-file/13,610,686-byte builds. The 156-file clean-install package and required Chromium/LibreOffice/Poppler gates pass in both environments. Hosted evidence is run [`29434484910`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29434484910).
- The newer PPTX native-object graph gate supersedes those counts: OpenChestnut 142/142, OfficeBridge 5/5, the 158-file clean install, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA pass locally and on hosted Linux. The local runtime is 38 files/13,623,484 bytes; hosted Linux produces two byte-identical 38-file/13,623,486-byte builds. Hosted evidence is run [`29437234633`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29437234633).
- The newer PPTX native-object placement gate supersedes the graph-only runtime/package evidence: name/outer-frame edits for recognized OLE/diagram/contentPart objects, immutable recursive payloads, JS fallback parity, the 158-file clean install, OpenChestnut 142/142, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA pass locally and on hosted Linux. The local runtime is 38 files/13,627,580 bytes; hosted Linux produces two byte-identical 38-file/13,627,582-byte builds. Hosted evidence is run [`29439588455`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29439588455).
- The newer PPTX OLE embedded-workbook gate supersedes the placement-only package/runtime counts: one non-shared source-bound XLSX payload may be replaced while its path/type/relationship/preview and the rest of the native graph remain immutable; a package-wide inbound-relationship audit rejects targets shared within one owner or across slides. Both codec paths, the runnable skill, the 159-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA pass locally and on hosted Linux. The local runtime is 38 files/13,640,380 bytes; hosted Linux produces two byte-identical 38-file/13,640,382-byte builds. Hosted evidence is exact-SHA run [`29443385695`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29443385695) at `36c2fdfc6ebc36a26ecd4a33e2d8270e4cf52c3b`.
- The newer XLSX plot-level data-label gate supersedes those package/runtime counts: public field 16, both codec paths, source-bound add/change/remove, false-residual preservation, complex-label fail-closed behavior, SVG/inspect, the runnable spreadsheet skill, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA all pass locally and on hosted Linux. The local runtime is 38 files/13,648,060 bytes; hosted Linux produces two byte-identical 38-file/13,648,062-byte builds. Hosted evidence is exact-SHA run [`29445489984`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29445489984) at `0e9ef96b372279a610cdba895d4b8827145a5f9b`.
- The newer XLSX plot-level data-label-position gate supersedes those package/runtime counts: optional public field 3, all nine standard positions plus JS short aliases, both codec paths, source-bound block/position add/change/remove, valid point-override fail-closed behavior, SVG/inspect, the runnable spreadsheet skill, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA all pass locally and on hosted Linux. The local runtime is 38 files/13,653,180 bytes; hosted Linux produces two byte-identical 38-file/13,653,182-byte builds. Hosted evidence is exact-SHA run [`29447394093`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29447394093) at `88f52e03447622b85bc718c44db81f0e43a8d36b`.
- The newer XLSX plot-level series-name-label gate supersedes those package/runtime counts: optional public field 4 preserves absent/false/true, both codec paths support source-bound add/change/remove, SVG/inspect compose the series name, the runnable skill crosses true→explicit-false, and the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA all pass locally and on hosted Linux. The local runtime is 38 files/13,654,716 bytes; hosted Linux produces two byte-identical 38-file/13,654,718-byte builds. Hosted evidence is exact-SHA run [`29448661598`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29448661598) at `423dc6a423bf605cb025a802a4157c6041030ddb`.
- The newer PPTX direct-background-removal gate supersedes those package/runtime counts: direct and effective backgrounds stay distinct, recognized Master/Layout `p:bg` nodes can be removed or re-added, complex native graphs reject explicit clear intent, and the runnable preservation skill, 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA all pass locally and on hosted Linux. The local runtime is 38 files/13,654,204 bytes; hosted Linux produces two byte-identical 38-file/13,654,206-byte builds. Hosted evidence is exact-SHA run [`29450481746`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29450481746) at `dceaf5898e4b1a001c3b302409f9a71c2f9b39b9`.
- The newer PPTX placeholder direct-frame gate supersedes those package/runtime counts: additive public field 7 atomically distinguishes recognized existing direct geometry from inherited or complex transforms; Master and Layout placeholders move/resize through bundled WASM while rotation and every other shape residual remain protected, and add/remove, zero-size, inherited-flattening, and complex-transform attempts fail closed. All npm/runnable-skill/docs/protobuf/clean-install gates pass locally with OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1/Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,890,717 bytes compressed and 16,453,765 bytes unpacked, leaving 246,235 bytes below the unchanged unpacked-size gate. The source-built macOS runtime contains 38 files/13,659,324 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29452274325`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29452274325) passes the same full gate at `f836edcc1ec3285ec804668f401c6c57da5b09d0` with real LibreOffice 24.2.7.2/Poppler 24.02.0 and two byte-identical 38-file/13,659,326-byte runtime builds.
- The follow-up PPTX placeholder direct-transform gate extends the same atomic public message with optional signed rotation and horizontal/vertical flip fields 5–7; native absence stays distinct from explicit zero/false, and `undefined` removes only those owned attributes without deleting the frame. Master and Layout placeholders move/resize/rotate/flip through bundled WASM; invalid, empty, inherited, complex, and native out-of-range transform edits fail closed while fill, preset geometry, shape style, and every other residual remain protected. Full local npm/runnable-skill/docs/protobuf/clean-install gates pass with OpenChestnut 143/143, OfficeBridge 5/5, Playwright 1.61.1/Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,893,207 bytes compressed and 16,463,358 bytes unpacked, leaving 236,642 bytes below the unchanged unpacked-size gate. The source-built macOS runtime contains 38 files/13,661,372 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29454062249`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29454062249) passes the same full gate at `2e3231f91500d39acfd924a54253c3dcd321b37c` with real LibreOffice 24.2.7.2/Poppler 24.02.0, the 160-file clean install, OpenChestnut 143/143, OfficeBridge 5/5, and two byte-identical 38-file/13,661,374-byte runtime builds.
- The newer PPTX placeholder frame-presence gate adds source-evidence field 5 to `PresentationElementSourceBinding` without duplicating frame state: `direct_frame` remains the only semantic value, while the binding permits presence changes only for an absent transform under existing `p:spPr` or one fully owned standard `a:xfrm`. Master absent→direct and Layout direct→absent edits pass JavaScript fallback, C#, bundled WASM, exact XML, second-import, runnable Playwright/LibreOffice/Poppler, and PATH-without-dotnet clean-install checks. Unknown attributes remain preserved during coordinate edits but make frame deletion fail closed; complex/partial/out-of-range transforms, invalid frames, transform-without-frame input, and binding tampering also reject. Full local npm/docs/protobuf/package/reproducibility gates pass with OpenChestnut 144/144, OfficeBridge 5/5, Playwright 1.61.1/Chromium 149.0.7827.55, LibreOfficeDev 26.8.0.0.alpha0, and Poppler 26.05.0. The 160-file package is 4,895,230 bytes compressed and 16,468,147 bytes unpacked, leaving 231,853 bytes below the unchanged 16,700,000-byte gate. The source-built macOS runtime contains 38 files/13,662,908 bytes, and two isolated builds are byte-identical across all 39 audited files. Exact-SHA hosted Linux run [`29455947947`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29455947947) passes the same full gate at `dae922836cc34d8a2f06f0d82d6aaa0265f21f0d` with Chromium 149.0.7827.55, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 144/144, OfficeBridge 5/5, and two byte-identical 38-file/13,662,910-byte runtime builds.
- The newer PPTX slide-placeholder effective-transform gate supersedes the position-only evidence: OpenChestnut exposes source identity/direct-frame facts, JavaScript resolves slide→layout/master by `idx`, projects effective position/rotation/flips plus provenance, and unchanged export retains an absent slide `a:xfrm`; a direct frame is atomic, while source-free OpenChestnut transform authoring and effective-projection mutation reject. Both local and exact-SHA hosted gates pass with the 160-file clean install, OpenChestnut 146/146, OfficeBridge 5/5, byte-identical 39-file reproducibility audit, and real browser/LibreOffice/Poppler QA. Hosted evidence is run [`29458982343`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29458982343) at `a19a4e94a7c166d264ace570fd5d6af107700544`.
- The newer PPTX top-level-picture gate additively models one exact embedded rectangular `p:pic` profile through independent public image/transform messages. Source-free authoring, source import, name/alt/frame/rotation/flip edits, same-format byte replacement, residual preservation, complex-profile fallback, cross-format rejection, JavaScript fallback, clean install, and the runnable Playwright/LibreOffice/Poppler fixture pass locally. Replacement keeps the old ImagePart/relationship and adds a tracked target, avoiding mutation of shared source media. OpenChestnut passes 148/148 and OfficeBridge 5/5; two isolated builds are byte-identical across all 39 audited files around a 38-file/13,687,484-byte runtime. The 160-file package is 4,906,725 bytes compressed and 16,519,844 bytes unpacked, leaving 180,156 bytes below the unchanged 16,700,000-byte gate. Exact-SHA hosted Linux run [`29461520236`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29461520236) passes the same complete npm/skill/render/docs/proto/package gate at `93aced2db8e1594e6793b0f356ed8b5fd13613c1` with real Chromium, LibreOffice 24.2.7.2, Poppler 24.02.0, the 160-file clean install, OpenChestnut 148/148, OfficeBridge 5/5, and two byte-identical 38-file/13,687,486-byte runtime builds.
- Playwright renderer smoke tests pass locally with Playwright 1.61.1 and its Chromium runtime, covering PNG, WebP, JPEG, and PDF output.
- The marker increment's complete local and exact-SHA hosted gates pass: OpenChestnut 134/134, OfficeBridge 5/5, all npm/runnable-skill tests, generated API/protobuf checks, the 154-file clean install, two byte-identical runtime builds, and required real browser/native rendering. Hosted evidence is run [`29410193061`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29410193061).
- The line-smoothing increment's complete local and exact-SHA hosted gates pass: OpenChestnut 135/135, OfficeBridge 5/5, all npm/runnable-skill tests, generated API/protobuf checks, the 155-file clean install, two byte-identical runtime builds, and required real browser/native rendering. Hosted evidence is run [`29411788218`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29411788218).
- The line-grouping increment's complete local and exact-SHA hosted gates pass: OpenChestnut 135/135, OfficeBridge 5/5, all npm/runnable-skill tests, generated API/protobuf checks, the 155-file clean install, two byte-identical runtime builds, and required real browser/native rendering. Hosted evidence is run [`29413451804`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29413451804).
- The line-vary-colors increment's complete local and exact-SHA hosted gates pass: OpenChestnut 135/135, OfficeBridge 5/5, all npm/runnable-skill tests, generated API/protobuf checks, the 155-file clean install, two byte-identical runtime builds, and required real browser/native rendering. Hosted evidence is run [`29414913314`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29414913314).
- The explicit Office codec-policy increment's complete local and exact-SHA hosted gates pass: all six main file-facade import/export methods share the public selector, the three runnable Office skills use it for OpenChestnut roundtrips, and the 156-file no-local-dotnet package resolves the delayed adapter. Hosted evidence is run [`29429961856`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29429961856).
- The styled line-marker increment's complete local and exact-SHA hosted gates pass: public wire fields, JavaScript fallback, bundled WASM, source-bound add/change/remove, fail-closed complex graphs, SVG preview, Office 2021 validation, clean install, reproducibility, and real browser/native spreadsheet QA all exercise the same marker projection. Hosted evidence is run [`29432070847`](https://github.com/w31r4/open-office-artifact-tool/actions/runs/29432070847).

## Publish command once auth is available

```sh
npm publish --access public
```

Do not publish if `npm run release:check` reports any required blocker, or if the current `package.json` version is already published.

## GitHub release workflow

A manual `.github/workflows/release.yml` workflow is available:

- `publish_npm=false` runs the release gates and `release-check` in dry-run/no-network mode.
- `publish_npm=true` requires `secrets.NPM_TOKEN`, runs `npm run release:check`, creates/pushes tag `v<package.version>`, creates a GitHub release if needed, and runs `npm publish --access public`.

Use `publish_npm=true` only after confirming the package version, changelog/release notes, and npm auth are correct.
